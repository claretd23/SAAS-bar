diff --git a/backend/routes/products.js b/backend/routes/products.js
index b5c198a..bc2c0f7 100644
--- a/backend/routes/products.js
+++ b/backend/routes/products.js
@@ -9,7 +9,6 @@ import { authMiddleware, requireRole } from "../middleware/auth.js";
 
 const __dirname = path.dirname(fileURLToPath(import.meta.url));
 
-// Carpeta donde se guardan las imágenes
 const UPLOADS_DIR = path.join(__dirname, "../../uploads");
 if (!fs.existsSync(UPLOADS_DIR)) fs.mkdirSync(UPLOADS_DIR, { recursive: true });
 
@@ -23,22 +22,15 @@ const storage = multer.diskStorage({
 
 const upload = multer({
   storage,
-  limits: { fileSize: 3 * 1024 * 1024 }, // 3MB máx
+  limits: { fileSize: 3 * 1024 * 1024 },
   fileFilter: (req, file, cb) => {
-    const allowedExt = [".jpg", ".jpeg", ".png", ".webp"];
+    const allowedExt  = [".jpg", ".jpeg", ".png", ".webp"];
     const allowedMime = ["image/jpeg", "image/jpg", "image/png", "image/webp"];
     const ext = path.extname(file.originalname).toLowerCase();
-
-    // Validamos por extensión O por mimetype. Algunos navegadores/clientes
-    // (capturas de pantalla en Windows, pegado desde portapapeles, ciertos
-    // apps de cámara) no ponen extensión en el nombre del archivo o la
-    // ponen distinta al contenido real — pero el mimetype que manda el
-    // navegador suele ser correcto. Con solo extensión, esos casos se
-    // rechazaban aunque la imagen fuera válida.
     if (allowedExt.includes(ext) || allowedMime.includes(file.mimetype)) {
       cb(null, true);
     } else {
-      cb(new Error(`Solo se permiten imágenes JPG, PNG o WebP (recibido: ${file.mimetype || "tipo desconocido"}, nombre: ${file.originalname})`));
+      cb(new Error(`Solo se permiten imagenes JPG, PNG o WebP (recibido: ${file.mimetype || "tipo desconocido"}, nombre: ${file.originalname})`));
     }
   },
 });
@@ -46,32 +38,22 @@ const upload = multer({
 const router = express.Router();
 router.use(authMiddleware);
 
-// Envuelve upload.single("image") para capturar errores de Multer
-// (fileFilter rechazado, archivo muy grande, etc.) y devolverlos como JSON
-// limpio con status 400, en vez de dejar que truenen como excepción sin
-// manejar — antes esos errores solo se veían como stack trace en la
-// consola del servidor y el frontend recibía un error genérico.
 function uploadImage(req, res, next) {
   upload.single("image")(req, res, (err) => {
     if (!err) return next();
     if (err instanceof multer.MulterError) {
-      if (err.code === "LIMIT_FILE_SIZE") {
-        return res.status(400).json({ error: "La imagen no debe superar 3MB" });
-      }
+      if (err.code === "LIMIT_FILE_SIZE") return res.status(400).json({ error: "La imagen no debe superar 3MB" });
       return res.status(400).json({ error: `Error subiendo imagen: ${err.message}` });
     }
-    // Errores lanzados manualmente desde fileFilter (cb(new Error(...)))
-    return res.status(400).json({ error: err.message || "Archivo no válido" });
+    return res.status(400).json({ error: err.message || "Archivo no valido" });
   });
 }
 
-// Helper para construir la URL pública de la imagen
 function imageUrl(req, filename) {
   if (!filename) return null;
   return `${req.protocol}://${req.get("host")}/uploads/${filename}`;
 }
 
-// Helper para borrar imagen anterior del disco
 function deleteImage(imageUrl) {
   if (!imageUrl) return;
   const filename = imageUrl.split("/uploads/").pop();
@@ -79,6 +61,11 @@ function deleteImage(imageUrl) {
   if (fs.existsSync(filepath)) fs.unlinkSync(filepath);
 }
 
+// Asegurar columna unlimited_stock existe (migracion segura)
+try {
+  db.prepare("ALTER TABLE products ADD COLUMN unlimited_stock INTEGER NOT NULL DEFAULT 0").run();
+} catch (_) { /* columna ya existe */ }
+
 // GET — todos los productos del negocio
 router.get("/", (req, res) => {
   const products = db
@@ -93,7 +80,7 @@ router.post(
   requireRole("admin", "superadmin"),
   uploadImage,
   (req, res) => {
-    const { name, cat, price, stock, emoji } = req.body;
+    const { name, cat, price, stock, unlimited_stock } = req.body;
     if (!name || !cat || price == null) {
       if (req.file) fs.unlinkSync(req.file.path);
       return res.status(400).json({ error: "name, cat y price son obligatorios" });
@@ -101,10 +88,11 @@ router.post(
     const id = nanoid(10);
     const filename = req.file ? req.file.filename : null;
     const imgUrl = filename ? `/uploads/${filename}` : null;
+    const unlimitedVal = unlimited_stock === "true" || unlimited_stock === true ? 1 : 0;
 
     db.prepare(
-      "INSERT INTO products (id, business_id, name, cat, price, stock, emoji, image_url) VALUES (?, ?, ?, ?, ?, ?, ?, ?)"
-    ).run(id, req.user.businessId, name, cat, +price, +stock || 0, emoji || "🍹", imgUrl);
+      "INSERT INTO products (id, business_id, name, cat, price, stock, emoji, image_url, unlimited_stock) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)"
+    ).run(id, req.user.businessId, name, cat, +price, unlimitedVal ? 0 : (+stock || 0), "", imgUrl, unlimitedVal);
 
     const product = db.prepare("SELECT * FROM products WHERE id = ?").get(id);
     req.app.get("io").to(req.user.businessId).emit("products_updated");
@@ -126,29 +114,31 @@ router.put(
       return res.status(404).json({ error: "Producto no encontrado" });
     }
 
-    const { name, cat, price, stock, emoji, remove_image } = req.body;
+    const { name, cat, price, stock, unlimited_stock, remove_image } = req.body;
 
     let imgUrl = existing.image_url;
-
     if (req.file) {
-      // Subió imagen nueva → borrar la vieja
       deleteImage(existing.image_url);
       imgUrl = `/uploads/${req.file.filename}`;
     } else if (remove_image === "true") {
-      // Pidió quitar la imagen
       deleteImage(existing.image_url);
       imgUrl = null;
     }
 
+    const unlimitedVal = unlimited_stock === "true" || unlimited_stock === true ? 1
+                       : unlimited_stock === "false" || unlimited_stock === false ? 0
+                       : existing.unlimited_stock;
+
     db.prepare(
-      "UPDATE products SET name=?, cat=?, price=?, stock=?, emoji=?, image_url=? WHERE id=?"
+      "UPDATE products SET name=?, cat=?, price=?, stock=?, emoji=?, image_url=?, unlimited_stock=? WHERE id=?"
     ).run(
-      name ?? existing.name,
-      cat ?? existing.cat,
+      name  ?? existing.name,
+      cat   ?? existing.cat,
       price != null ? +price : existing.price,
-      stock != null ? +stock : existing.stock,
-      emoji ?? existing.emoji,
+      unlimitedVal ? 0 : (stock != null ? +stock : existing.stock),
+      "", // emoji ya no se usa en la UI pero se conserva el campo
       imgUrl,
+      unlimitedVal,
       req.params.id
     );
 
@@ -165,25 +155,29 @@ router.delete("/:id", requireRole("admin", "superadmin"), (req, res) => {
   if (!existing) return res.status(404).json({ error: "Producto no encontrado" });
 
   deleteImage(existing.image_url);
-  db.prepare("DELETE FROM products WHERE id = ?").run(req.params.id);
+  db.prepare("DELETE FROM products WHERE id = ?\n").run(req.params.id);
   req.app.get("io").to(req.user.businessId).emit("products_updated");
   res.json({ ok: true });
 });
 
-// PATCH — ajustar stock
+// PATCH /:id/stock — ajustar cantidad (ignora si unlimited_stock)
 router.patch("/:id/stock", requireRole("admin", "superadmin"), (req, res) => {
   const product = db
     .prepare("SELECT * FROM products WHERE id = ? AND business_id = ?")
     .get(req.params.id, req.user.businessId);
   if (!product) return res.status(404).json({ error: "Producto no encontrado" });
 
+  if (product.unlimited_stock) {
+    return res.json({ ok: true, stock: null, unlimited_stock: true });
+  }
+
   let newStock;
   if (req.body.set != null) {
     newStock = Math.max(0, +req.body.set);
   } else if (req.body.delta != null) {
     newStock = Math.max(0, product.stock + +req.body.delta);
   } else {
-    return res.status(400).json({ error: "Envía 'set' o 'delta'" });
+    return res.status(400).json({ error: "Envia 'set' o 'delta'" });
   }
 
   db.prepare("UPDATE products SET stock = ? WHERE id = ?").run(newStock, req.params.id);
@@ -191,4 +185,4 @@ router.patch("/:id/stock", requireRole("admin", "superadmin"), (req, res) => {
   res.json({ ok: true, stock: newStock });
 });
 
-export default router;
\ No newline at end of file
+export default router;
diff --git a/frontend/src/components/AdminView.jsx b/frontend/src/components/AdminView.jsx
index cbd243f..9c622fa 100644
--- a/frontend/src/components/AdminView.jsx
+++ b/frontend/src/components/AdminView.jsx
@@ -1,20 +1,32 @@
 import { useState, useEffect } from "react";
 import { C, fmt, today } from "../styles.js";
 import { api } from "../api.js";
-import { Btn, Badge, Card, Modal, Input, ErrorBanner, Divider } from "./Common.jsx";
+import { Btn, Badge, Card, Modal, Input, ErrorBanner, Divider, Icon } from "./Common.jsx";
 
 const TABS = [
-  { id: "dashboard", label: "📊 Dashboard" },
-  { id: "orders", label: "🧾 Mesas" },
-  { id: "menu", label: "🍹 Menú" },
-  { id: "promos", label: "🏷️ Promos" },
-  { id: "inventory", label: "📦 Inventario" },
-  { id: "users", label: "👥 Usuarios" },
+  { id: "dashboard", label: "Dashboard", icon: "admin" },
+  { id: "orders",    label: "Mesas",     icon: "table"  },
+  { id: "menu",      label: "Menu",      icon: "bar"    },
+  { id: "promos",    label: "Promos",    icon: "tag"    },
+  { id: "inventory", label: "Inventario",icon: "box"    },
+  { id: "users",     label: "Usuarios",  icon: "users"  },
+  { id: "config",    label: "Config",    icon: "settings"},
 ];
 
 export default function AdminView({ user, products, promos, orders, onOrdersChanged, onProductsChanged, onPromosChanged, onLogout }) {
   const [tab, setTab] = useState("dashboard");
   const [dashboard, setDashboard] = useState(null);
+  // mesas/bancos configurables: persiste en localStorage por negocio
+  const storageKey = `bar_config_${user.businessId}`;
+  const loadConfig = () => {
+    try { return JSON.parse(localStorage.getItem(storageKey)) || { mesas: 10, bancos: 4 }; }
+    catch { return { mesas: 10, bancos: 4 }; }
+  };
+  const [venueConfig, setVenueConfig] = useState(loadConfig);
+  const saveConfig = (cfg) => {
+    setVenueConfig(cfg);
+    localStorage.setItem(storageKey, JSON.stringify(cfg));
+  };
 
   useEffect(() => {
     if (tab === "dashboard") {
@@ -27,33 +39,37 @@ export default function AdminView({ user, products, promos, orders, onOrdersChan
       {/* Header */}
       <div style={{ background: C.bg2, borderBottom: `1px solid ${C.border}`, padding: "10px 14px", display: "flex", justifyContent: "space-between", alignItems: "center" }}>
         <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
-          <span style={{ fontSize: 20 }}>📊</span>
+          <Icon name="admin" size={20} color={C.neon} />
           <div>
             <div style={{ fontWeight: 600, color: C.neon, fontSize: 13 }}>ADMIN</div>
             <div style={{ fontSize: 11, color: C.muted }}>{today()}</div>
           </div>
         </div>
-        <Btn size="sm" variant="ghost" onClick={onLogout}>Salir</Btn>
+        <Btn size="sm" variant="ghost" onClick={onLogout}><Icon name="logout" size={13} /> Salir</Btn>
       </div>
 
       {/* Tabs */}
       <div style={{ background: C.bg2, borderBottom: `1px solid ${C.border}`, display: "flex", overflowX: "auto" }}>
         {TABS.map(t => (
           <button key={t.id} onClick={() => setTab(t.id)} style={{
-            padding: "10px 16px", border: "none", borderBottom: `2px solid ${tab === t.id ? C.neon : "transparent"}`,
+            padding: "10px 14px", border: "none", borderBottom: `2px solid ${tab === t.id ? C.neon : "transparent"}`,
             background: "transparent", color: tab === t.id ? C.neon : C.muted,
             fontSize: 12, cursor: "pointer", whiteSpace: "nowrap", fontFamily: "inherit", fontWeight: 500,
-          }}>{t.label}</button>
+            display: "flex", alignItems: "center", gap: 5,
+          }}>
+            <Icon name={t.icon} size={13} />{t.label}
+          </button>
         ))}
       </div>
 
       <div style={{ flex: 1, overflowY: "auto", padding: 14 }}>
-        {tab === "dashboard" && <DashboardTab data={dashboard} orders={orders} />}
-        {tab === "orders" && <OrdersTab orders={orders} onChanged={onOrdersChanged} />}
-        {tab === "menu" && <MenuTab products={products} onChanged={onProductsChanged} />}
-        {tab === "promos" && <PromosTab promos={promos} onChanged={onPromosChanged} />}
-        {tab === "inventory" && <InventoryTab products={products} onChanged={onProductsChanged} />}
-        {tab === "users" && <UsersTab businessId={user.businessId} />}
+        {tab === "dashboard"  && <DashboardTab data={dashboard} orders={orders} />}
+        {tab === "orders"     && <OrdersTab orders={orders} onChanged={onOrdersChanged} />}
+        {tab === "menu"       && <MenuTab products={products} onChanged={onProductsChanged} />}
+        {tab === "promos"     && <PromosTab promos={promos} onChanged={onPromosChanged} />}
+        {tab === "inventory"  && <InventoryTab products={products} onChanged={onProductsChanged} />}
+        {tab === "users"      && <UsersTab businessId={user.businessId} />}
+        {tab === "config"     && <ConfigTab config={venueConfig} onSave={saveConfig} />}
       </div>
     </div>
   );
@@ -62,14 +78,16 @@ export default function AdminView({ user, products, promos, orders, onOrdersChan
 /* ─── Dashboard ─── */
 function DashboardTab({ data, orders }) {
   if (!data) return <div style={{ textAlign: "center", color: C.muted, marginTop: 40 }}>Cargando...</div>;
+  const PAY_ICON = { ef: "cash", ta: "card", qr: "qr" };
+  const PAY_NAME = { ef: "Efectivo", ta: "Tarjeta", qr: "QR/Trans." };
   return (
     <div className="fade-in">
       <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(160px, 1fr))", gap: 10, marginBottom: 14 }}>
         {[
-          { label: "Ventas hoy", value: fmt(data.totalToday || 0), color: C.neon },
-          { label: "Pagos cobrados", value: data.ordersToday || 0, color: C.blue },
-          { label: "Mesas activas", value: data.activeOrders ?? orders.filter(o => !o.is_closed).length, color: C.amber },
-          { label: "Ticket promedio", value: fmt(data.avgTicket || 0), color: C.neon2 },
+          { label: "Ventas hoy",    value: fmt(data.totalToday || 0),                                       color: C.neon  },
+          { label: "Pagos cobrados",value: data.ordersToday || 0,                                           color: C.blue  },
+          { label: "Mesas activas", value: data.activeOrders ?? orders.filter(o => !o.is_closed).length,    color: C.amber },
+          { label: "Ticket promedio",value: fmt(data.avgTicket || 0),                                       color: C.neon2 },
         ].map(stat => (
           <Card key={stat.label}>
             <div style={{ fontSize: 11, color: C.muted, marginBottom: 4 }}>{stat.label}</div>
@@ -83,7 +101,7 @@ function DashboardTab({ data, orders }) {
           {data.topProducts.map((p, i) => (
             <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 10, padding: "7px 0", borderBottom: `1px solid ${C.border}` }}>
               <span style={{ color: C.muted, fontSize: 11, minWidth: 16 }}>#{i + 1}</span>
-              <span style={{ fontSize: 16 }}>{p.emoji}</span>
+              <Icon name="bar" size={16} color={C.muted} />
               <span style={{ flex: 1, fontSize: 13 }}>{p.name}</span>
               <Badge color={C.neon}>{p.qty} vendidos</Badge>
               <span style={{ color: C.neon, fontSize: 13 }}>{fmt(p.total)}</span>
@@ -93,10 +111,13 @@ function DashboardTab({ data, orders }) {
       )}
       {data.byPayMethod?.length > 0 && (
         <div style={{ marginTop: 14 }}>
-          <div style={{ fontSize: 12, color: C.muted, marginBottom: 8, textTransform: "uppercase", letterSpacing: 1 }}>Por método de pago</div>
+          <div style={{ fontSize: 12, color: C.muted, marginBottom: 8, textTransform: "uppercase", letterSpacing: 1 }}>Por metodo de pago</div>
           {data.byPayMethod.map(m => (
-            <div key={m.pay} style={{ display: "flex", justifyContent: "space-between", padding: "5px 0", fontSize: 13, borderBottom: `1px solid ${C.border}` }}>
-              <span>{m.pay === "ef" ? "💵 Efectivo" : m.pay === "ta" ? "💳 Tarjeta" : "📱 QR/Trans."}</span>
+            <div key={m.pay} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "5px 0", fontSize: 13, borderBottom: `1px solid ${C.border}` }}>
+              <span style={{ display: "flex", alignItems: "center", gap: 6 }}>
+                <Icon name={PAY_ICON[m.pay] || "cash"} size={14} color={C.muted} />
+                {PAY_NAME[m.pay] || m.pay}
+              </span>
               <span style={{ color: C.neon }}>{fmt(m.total)}</span>
             </div>
           ))}
@@ -137,17 +158,25 @@ function OrdersTab({ orders, onChanged }) {
             <div style={{ marginBottom: 6 }}>
               {order.items.map((item, idx) => (
                 <div key={idx} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "3px 0", fontSize: 12, opacity: item.paid ? 0.45 : 1 }}>
-                  <span>{item.qty}× {item.name} {item.paid && "✓"}</span>
+                  <span style={{ display: "flex", alignItems: "center", gap: 4 }}>
+                    {item.qty}x {item.name} {item.paid && <Icon name="check" size={11} color={C.neon} />}
+                  </span>
                   <Badge color={STATUS_COLOR[item.status] || C.muted}>{STATUS_LABEL[item.status] || item.status}</Badge>
                 </div>
               ))}
             </div>
-            {order.note && <div style={{ fontSize: 11, color: C.amber, marginBottom: 6 }}>📝 {order.note}</div>}
+            {order.note && (
+              <div style={{ display: "flex", alignItems: "center", gap: 5, fontSize: 11, color: C.amber, marginBottom: 6 }}>
+                <Icon name="note" size={12} color={C.amber} />{order.note}
+              </div>
+            )}
             <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
               <span style={{ fontWeight: 600, color: C.neon }}>{fmt(order.total)}</span>
               <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                 {paidCount > 0 && <Badge color={C.neon}>{paidCount} pagado{paidCount > 1 ? "s" : ""}</Badge>}
-                <Btn size="sm" variant="amber" onClick={() => setPayModal(order)}>💳 Cobrar</Btn>
+                <Btn size="sm" variant="amber" onClick={() => setPayModal(order)}>
+                  <Icon name="card" size={12} /> Cobrar
+                </Btn>
               </div>
             </div>
           </Card>
@@ -173,39 +202,44 @@ function OrdersTab({ orders, onChanged }) {
       )}
 
       {payModal && (
-        <AdminPayModal order={payModal} onClose={() => setPayModal(null)} onPaid={() => { setPayModal(null); onChanged(); }} />
+        <PayModal order={payModal} onClose={() => setPayModal(null)} onPaid={() => { setPayModal(null); onChanged(); }} />
       )}
     </div>
   );
 }
 
-const PAY_METHODS = [
-  { id: "ef", label: "Efectivo" },
-  { id: "ta", label: "Tarjeta" },
-  { id: "qr", label: "QR/Trans." },
+/* ─── Modal de cobro compartido (Admin + Barman) ─── */
+export const PAY_METHODS = [
+  { id: "ef",  label: "Efectivo", icon: "cash" },
+  { id: "ta",  label: "Tarjeta",  icon: "card" },
+  { id: "qr",  label: "QR/Trans.",icon: "qr"   },
 ];
 
-// Mismo flujo de cobro parcial que tiene el mesero: el admin puede cobrar
-// cualquier mesa desde aquí (ej. si el mesero está ocupado), eligiendo qué
-// items se pagan y con qué método.
-function AdminPayModal({ order, onClose, onPaid }) {
+export function PayModal({ order, onClose, onPaid }) {
   const pendingIdx = order.items.map((it, i) => ({ it, i })).filter(x => !x.it.paid).map(x => x.i);
   const [selected, setSelected] = useState(new Set(pendingIdx));
   const [pay, setPay] = useState("ef");
+  const [received, setReceived] = useState("");
   const [saving, setSaving] = useState(false);
   const [error, setError] = useState("");
+  const [paid, setPaid] = useState(false);
 
   const toggle = (idx) => {
     setSelected(s => { const n = new Set(s); n.has(idx) ? n.delete(idx) : n.add(idx); return n; });
   };
   const selectedTotal = [...selected].reduce((s, idx) => s + order.items[idx].price * order.items[idx].qty, 0);
+  const receivedNum = parseFloat(received) || 0;
+  const change = pay === "ef" && receivedNum > 0 ? receivedNum - selectedTotal : null;
 
   const confirmPay = async () => {
     if (!selected.size) { setError("Selecciona al menos un producto a cobrar"); return; }
+    if (pay === "ef" && received && receivedNum < selectedTotal) {
+      setError(`El monto recibido ($${receivedNum.toFixed(2)}) es menor al total`); return;
+    }
     setSaving(true); setError("");
     try {
       await api.payOrderItems(order.id, pay, [...selected]);
-      onPaid();
+      setPaid(true);
     } catch (e) {
       setError(e.message);
     } finally {
@@ -213,28 +247,128 @@ function AdminPayModal({ order, onClose, onPaid }) {
     }
   };
 
+  const printTicket = () => {
+    const lines = [
+      "================================",
+      "          TICKET DE COBRO       ",
+      "================================",
+      `Mesa/Barra: ${order.mesa}`,
+      `Fecha: ${new Date().toLocaleString("es-MX")}`,
+      "--------------------------------",
+      ...order.items.filter((_, i) => selected.has(i)).map(it =>
+        `${it.qty}x ${it.name.padEnd(16)} ${fmt(it.price * it.qty)}`
+      ),
+      "--------------------------------",
+      `TOTAL:                 ${fmt(selectedTotal)}`,
+      pay === "ef" && receivedNum > 0 ? `Recibido:              ${fmt(receivedNum)}` : null,
+      pay === "ef" && change !== null && change >= 0 ? `Cambio:                ${fmt(change)}` : null,
+      `Metodo: ${PAY_METHODS.find(m => m.id === pay)?.label || pay}`,
+      "================================",
+      "       Gracias por su visita    ",
+      "================================",
+    ].filter(Boolean).join("\n");
+
+    const w = window.open("", "_blank", "width=350,height=500");
+    w.document.write(`<pre style="font-family:monospace;font-size:13px;padding:16px">${lines}</pre>`);
+    w.document.close();
+    w.print();
+  };
+
+  const isBarra = /^B\d+$/.test(String(order.mesa));
+  const title = `Cobrar ${isBarra ? "Barra" : "Mesa"} ${order.mesa}`;
+
+  if (paid) {
+    return (
+      <Modal title={title} onClose={onPaid}>
+        <div style={{ textAlign: "center", padding: "10px 0 20px" }}>
+          <div style={{ color: C.neon, marginBottom: 8 }}><Icon name="check" size={40} /></div>
+          <div style={{ fontWeight: 600, fontSize: 15, marginBottom: 4 }}>Cobro registrado</div>
+          <div style={{ fontSize: 13, color: C.muted, marginBottom: 12 }}>{fmt(selectedTotal)} — {PAY_METHODS.find(m => m.id === pay)?.label}</div>
+          {pay === "ef" && change !== null && change >= 0 && (
+            <div style={{ background: C.neon + "22", border: `1px solid ${C.neon}`, borderRadius: 10, padding: "10px 14px", marginBottom: 14 }}>
+              <div style={{ fontSize: 11, color: C.muted, marginBottom: 2 }}>Cambio a entregar</div>
+              <div style={{ fontSize: 28, fontWeight: 700, color: C.neon }}>{fmt(change)}</div>
+            </div>
+          )}
+        </div>
+        <div style={{ display: "flex", gap: 8 }}>
+          <Btn variant="ghost" onClick={printTicket} style={{ flex: 1 }}>
+            <Icon name="receipt" size={13} /> Ticket
+          </Btn>
+          <Btn variant="primary" onClick={onPaid} style={{ flex: 1 }}>Cerrar</Btn>
+        </div>
+      </Modal>
+    );
+  }
+
   return (
-    <Modal title={`Cobrar ${/^B\d+$/.test(String(order.mesa)) ? "Barra" : "Mesa"} ${order.mesa}`} onClose={onClose}>
+    <Modal title={title} onClose={onClose}>
       <ErrorBanner message={error} />
-      <div style={{ maxHeight: 220, overflowY: "auto", marginBottom: 10 }}>
+      <div style={{ maxHeight: 200, overflowY: "auto", marginBottom: 10 }}>
         {order.items.map((item, idx) => (
           <label key={idx} style={{ display: "flex", alignItems: "center", gap: 8, padding: "6px 0", borderBottom: `1px solid ${C.border}`, opacity: item.paid ? 0.4 : 1, cursor: item.paid ? "default" : "pointer" }}>
             <input type="checkbox" checked={item.paid || selected.has(idx)} disabled={item.paid} onChange={() => toggle(idx)} />
-            <span style={{ flex: 1, fontSize: 12 }}>{item.qty}× {item.name} {item.paid && <span style={{ color: C.neon, fontSize: 10 }}>(pagado)</span>}</span>
+            <span style={{ flex: 1, fontSize: 12 }}>
+              {item.qty}x {item.name} {item.paid && <span style={{ color: C.neon, fontSize: 10 }}>(pagado)</span>}
+            </span>
             <span style={{ fontSize: 12, color: C.neon }}>{fmt(item.price * item.qty)}</span>
           </label>
         ))}
       </div>
-      <div style={{ display: "flex", gap: 4, marginBottom: 12 }}>
+
+      {/* Metodos de pago */}
+      <div style={{ display: "flex", gap: 4, marginBottom: 10 }}>
         {PAY_METHODS.map(m => (
           <button key={m.id} onClick={() => setPay(m.id)} style={{
-            flex: 1, padding: "6px 2px", borderRadius: 6, fontSize: 11,
+            flex: 1, padding: "7px 4px", borderRadius: 6, fontSize: 11,
             background: pay === m.id ? C.neon2 + "22" : "transparent",
             border: `1px solid ${pay === m.id ? C.neon2 : C.border}`,
             color: pay === m.id ? C.neon2 : C.muted, cursor: "pointer",
-          }}>{m.label}</button>
+            display: "flex", alignItems: "center", justifyContent: "center", gap: 4,
+            fontFamily: "inherit",
+          }}>
+            <Icon name={m.icon} size={12} />{m.label}
+          </button>
         ))}
       </div>
+
+      {/* Calculadora de cambio — solo efectivo */}
+      {pay === "ef" && (
+        <div style={{ background: C.bg3, borderRadius: 8, padding: "10px 12px", marginBottom: 10 }}>
+          <div style={{ fontSize: 11, color: C.muted, marginBottom: 6 }}>Monto recibido (opcional)</div>
+          <div style={{ display: "flex", gap: 6, marginBottom: 6 }}>
+            {[selectedTotal, Math.ceil(selectedTotal / 100) * 100, Math.ceil(selectedTotal / 200) * 200].filter((v, i, a) => a.indexOf(v) === i).map(v => (
+              <button key={v} onClick={() => setReceived(String(v.toFixed(2)))} style={{
+                flex: 1, padding: "5px 2px", borderRadius: 6, fontSize: 11,
+                background: received === String(v.toFixed(2)) ? C.amber + "22" : C.bg4,
+                border: `1px solid ${received === String(v.toFixed(2)) ? C.amber : C.border}`,
+                color: received === String(v.toFixed(2)) ? C.amber : C.muted, cursor: "pointer", fontFamily: "inherit",
+              }}>{fmt(v)}</button>
+            ))}
+          </div>
+          <input
+            type="number"
+            placeholder="O escribe el monto..."
+            value={received}
+            onChange={e => setReceived(e.target.value)}
+            style={{ width: "100%", background: C.bg4, border: `1px solid ${C.border2}`, borderRadius: 6, color: C.text, padding: "6px 8px", fontSize: 12, outline: "none" }}
+          />
+          {change !== null && (
+            <div style={{ marginTop: 8, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
+              <span style={{ fontSize: 12, color: C.muted }}>Cambio</span>
+              <span style={{ fontSize: 18, fontWeight: 700, color: change < 0 ? C.red : C.neon }}>{fmt(Math.max(0, change))}</span>
+            </div>
+          )}
+        </div>
+      )}
+
+      {pay === "ta" && (
+        <div style={{ background: C.bg3, border: `1px solid ${C.border2}`, borderRadius: 8, padding: "10px 12px", marginBottom: 10, display: "flex", alignItems: "center", gap: 8 }}>
+          <Icon name="info" size={14} color={C.muted} />
+          <span style={{ fontSize: 11, color: C.muted }}>La integracion con terminal se configura proximamente.</span>
+        </div>
+      )}
+
       <Divider />
       <div style={{ display: "flex", justifyContent: "space-between", fontSize: 15, fontWeight: 600, marginBottom: 14 }}>
         <span>A cobrar ahora</span><span style={{ color: C.neon }}>{fmt(selectedTotal)}</span>
@@ -249,12 +383,12 @@ function AdminPayModal({ order, onClose, onPaid }) {
   );
 }
 
-/* ─── Menú ─── */
+/* ─── Menu ─── */
 function MenuTab({ products, onChanged }) {
   const API_URL = import.meta.env.VITE_API_URL || "http://localhost:4000";
   const [modal, setModal] = useState(null);
   const [form, setForm] = useState({
-    name: "", cat: "", price: "", stock: "", emoji: "🍹",
+    name: "", cat: "", price: "", stock: "", unlimited_stock: false,
     imageFile: null, previewUrl: null, remove_image: false,
   });
   const [error, setError] = useState("");
@@ -262,11 +396,12 @@ function MenuTab({ products, onChanged }) {
 
   const open = (p = null) => {
     setForm(p ? {
-      name: p.name, cat: p.cat, price: p.price, stock: p.stock, emoji: p.emoji,
+      name: p.name, cat: p.cat, price: p.price, stock: p.stock,
+      unlimited_stock: !!p.unlimited_stock,
       imageFile: null, previewUrl: p.image_url ? `${API_URL}${p.image_url}` : null,
       remove_image: false,
     } : {
-      name: "", cat: "", price: "", stock: "", emoji: "🍹",
+      name: "", cat: "", price: "", stock: "", unlimited_stock: false,
       imageFile: null, previewUrl: null, remove_image: false,
     });
     setModal(p || "new");
@@ -276,12 +411,7 @@ function MenuTab({ products, onChanged }) {
   const handleImageChange = (e) => {
     const file = e.target.files[0];
     if (!file) return;
-    setForm(f => ({
-      ...f,
-      imageFile: file,
-      previewUrl: URL.createObjectURL(file),
-      remove_image: false,
-    }));
+    setForm(f => ({ ...f, imageFile: file, previewUrl: URL.createObjectURL(file), remove_image: false }));
   };
 
   const removeImage = () => {
@@ -290,27 +420,20 @@ function MenuTab({ products, onChanged }) {
 
   const save = async () => {
     if (!form.name || !form.cat || !form.price) {
-      setError("Nombre, categoría y precio son obligatorios");
-      return;
+      setError("Nombre, categoria y precio son obligatorios"); return;
     }
     setSaving(true);
     try {
       const payload = {
-        name: form.name,
-        cat: form.cat,
-        price: form.price,
-        stock: form.stock || 0,
-        emoji: form.emoji,
+        name: form.name, cat: form.cat, price: form.price,
+        stock: form.unlimited_stock ? 0 : (form.stock || 0),
+        unlimited_stock: form.unlimited_stock ? "true" : "false",
         ...(form.imageFile ? { image: form.imageFile } : {}),
         ...(form.remove_image ? { remove_image: "true" } : {}),
       };
-      if (modal === "new") {
-        await api.createProduct(payload);
-      } else {
-        await api.updateProduct(modal.id, payload);
-      }
-      onChanged();
-      setModal(null);
+      if (modal === "new") await api.createProduct(payload);
+      else await api.updateProduct(modal.id, payload);
+      onChanged(); setModal(null);
     } catch (e) {
       setError(e.message);
     } finally {
@@ -319,7 +442,7 @@ function MenuTab({ products, onChanged }) {
   };
 
   const del = async (id) => {
-    if (!confirm("¿Eliminar producto?")) return;
+    if (!confirm("Eliminar producto?")) return;
     try { await api.deleteProduct(id); onChanged(); } catch (e) { alert(e.message); }
   };
 
@@ -329,7 +452,7 @@ function MenuTab({ products, onChanged }) {
     <div className="fade-in">
       <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}>
         <div style={{ fontSize: 13, fontWeight: 500 }}>{products.length} productos</div>
-        <Btn variant="primary" size="sm" onClick={() => open()}>+ Nuevo</Btn>
+        <Btn variant="primary" size="sm" onClick={() => open()}><Icon name="plus" size={13} /> Nuevo</Btn>
       </div>
 
       {cats.map(cat => (
@@ -337,21 +460,21 @@ function MenuTab({ products, onChanged }) {
           <div style={{ fontSize: 11, color: C.muted, textTransform: "uppercase", letterSpacing: 1, marginBottom: 6 }}>{cat}</div>
           {products.filter(p => p.cat === cat).map(p => (
             <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 10, padding: "7px 0", borderBottom: `1px solid ${C.border}` }}>
-              {/* Imagen o emoji */}
               {p.image_url ? (
-                <img
-                  src={`${API_URL}${p.image_url}`}
-                  alt={p.name}
-                  style={{ width: 36, height: 36, borderRadius: 8, objectFit: "cover", flexShrink: 0 }}
-                />
+                <img src={`${API_URL}${p.image_url}`} alt={p.name} style={{ width: 36, height: 36, borderRadius: 8, objectFit: "cover", flexShrink: 0 }} />
               ) : (
-                <span style={{ fontSize: 24, width: 36, textAlign: "center" }}>{p.emoji}</span>
+                <div style={{ width: 36, height: 36, borderRadius: 8, background: C.bg4, display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
+                  <Icon name="bar" size={18} color={C.muted} />
+                </div>
               )}
               <span style={{ flex: 1, fontSize: 13 }}>{p.name}</span>
               <span style={{ color: C.neon, fontSize: 13 }}>${Number(p.price).toFixed(2)}</span>
-              <Badge color={p.stock < 5 ? C.red : C.muted}>{p.stock} uds</Badge>
-              <Btn size="sm" variant="ghost" onClick={() => open(p)}>✏️</Btn>
-              <Btn size="sm" variant="ghost" onClick={() => del(p.id)}>🗑️</Btn>
+              {p.unlimited_stock
+                ? <Badge color={C.neon2}><Icon name="infinity" size={11} /></Badge>
+                : <Badge color={p.stock < 5 ? C.red : C.muted}>{p.stock} uds</Badge>
+              }
+              <Btn size="sm" variant="ghost" onClick={() => open(p)}><Icon name="edit" size={13} /></Btn>
+              <Btn size="sm" variant="ghost" onClick={() => del(p.id)}><Icon name="trash" size={13} /></Btn>
             </div>
           ))}
         </div>
@@ -360,44 +483,42 @@ function MenuTab({ products, onChanged }) {
       {modal && (
         <Modal title={modal === "new" ? "Nuevo producto" : "Editar producto"} onClose={() => setModal(null)}>
           <ErrorBanner message={error} />
-
-          {/* Preview de imagen */}
           <div style={{ marginBottom: 14 }}>
             {form.previewUrl ? (
-              <div style={{ position: "relative", display: "inline-block" }}>
-                <img
-                  src={form.previewUrl}
-                  alt="preview"
-                  style={{ width: "100%", maxHeight: 160, objectFit: "cover", borderRadius: 10, border: `1px solid ${C.border2}` }}
-                />
-                <button
-                  onClick={removeImage}
-                  style={{
-                    position: "absolute", top: 6, right: 6,
-                    background: "#000a", border: "none", color: "#fff",
-                    borderRadius: "50%", width: 24, height: 24, cursor: "pointer", fontSize: 12,
-                  }}
-                >✕</button>
+              <div style={{ position: "relative", display: "inline-block", width: "100%" }}>
+                <img src={form.previewUrl} alt="preview" style={{ width: "100%", maxHeight: 160, objectFit: "cover", borderRadius: 10, border: `1px solid ${C.border2}` }} />
+                <button onClick={removeImage} style={{ position: "absolute", top: 6, right: 6, background: "#000a", border: "none", color: "#fff", borderRadius: "50%", width: 24, height: 24, cursor: "pointer", fontSize: 12, display: "flex", alignItems: "center", justifyContent: "center" }}>
+                  <Icon name="x" size={12} />
+                </button>
               </div>
             ) : (
-              <label style={{
-                display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
-                height: 100, border: `2px dashed ${C.border2}`, borderRadius: 10,
-                color: C.muted, cursor: "pointer", fontSize: 12, gap: 6,
-              }}>
-                <span style={{ fontSize: 24 }}>📷</span>
+              <label style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", height: 100, border: `2px dashed ${C.border2}`, borderRadius: 10, color: C.muted, cursor: "pointer", fontSize: 12, gap: 6 }}>
+                <Icon name="camera" size={24} />
                 <span>Toca para subir imagen</span>
-                <span style={{ fontSize: 10 }}>JPG, PNG o WebP · máx 3 MB</span>
+                <span style={{ fontSize: 10 }}>JPG, PNG o WebP - max 3 MB</span>
                 <input type="file" accept="image/*" onChange={handleImageChange} style={{ display: "none" }} />
               </label>
             )}
           </div>
 
-          <Input label="Emoji (si no hay imagen)" value={form.emoji} onChange={e => setForm(f => ({ ...f, emoji: e.target.value }))} />
           <Input label="Nombre" value={form.name} onChange={e => setForm(f => ({ ...f, name: e.target.value }))} />
-          <Input label="Categoría" value={form.cat} onChange={e => setForm(f => ({ ...f, cat: e.target.value }))} placeholder="ej. Cócteles" />
+          <Input label="Categoria" value={form.cat} onChange={e => setForm(f => ({ ...f, cat: e.target.value }))} placeholder="ej. Cocteles" />
           <Input label="Precio" type="number" value={form.price} onChange={e => setForm(f => ({ ...f, price: e.target.value }))} />
-          <Input label="Stock inicial" type="number" value={form.stock} onChange={e => setForm(f => ({ ...f, stock: e.target.value }))} />
+
+          {/* Stock / ilimitado */}
+          <div style={{ marginBottom: 12 }}>
+            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
+              <div style={{ fontSize: 11, color: C.muted, textTransform: "uppercase", letterSpacing: ".5px" }}>Stock</div>
+              <label style={{ display: "flex", alignItems: "center", gap: 6, cursor: "pointer", fontSize: 12, color: form.unlimited_stock ? C.neon2 : C.muted }}>
+                <input type="checkbox" checked={form.unlimited_stock} onChange={e => setForm(f => ({ ...f, unlimited_stock: e.target.checked }))} style={{ accentColor: C.neon2 }} />
+                <Icon name="infinity" size={13} /> Sin limite
+              </label>
+            </div>
+            {!form.unlimited_stock && (
+              <input type="number" value={form.stock} onChange={e => setForm(f => ({ ...f, stock: e.target.value }))}
+                style={{ width: "100%", background: C.bg3, border: `1px solid ${C.border2}`, borderRadius: 8, color: C.text, padding: "8px 10px", fontSize: 13, outline: "none" }} />
+            )}
+          </div>
 
           <div style={{ display: "flex", gap: 8, marginTop: 4 }}>
             <Btn variant="ghost" onClick={() => setModal(null)} style={{ flex: 1 }}>Cancelar</Btn>
@@ -414,23 +535,19 @@ function MenuTab({ products, onChanged }) {
 /* ─── Promos ─── */
 function PromosTab({ promos, onChanged }) {
   const [modal, setModal] = useState(null);
-  const [form, setForm] = useState({ name: "", desc: "", type: "percent", discount: "", emoji: "🏷️" });
+  const [form, setForm] = useState({ name: "", desc: "", type: "percent", discount: "" });
   const [error, setError] = useState("");
 
   const open = (p = null) => {
-    setForm(p ? { name: p.name, desc: p.desc || "", type: p.type, discount: p.discount, emoji: p.emoji } : { name: "", desc: "", type: "percent", discount: "", emoji: "🏷️" });
-    setModal(p || "new");
-    setError("");
+    setForm(p ? { name: p.name, desc: p.desc || "", type: p.type, discount: p.discount } : { name: "", desc: "", type: "percent", discount: "" });
+    setModal(p || "new"); setError("");
   };
 
   const save = async () => {
     if (!form.name) { setError("El nombre es obligatorio"); return; }
     try {
-      if (modal === "new") {
-        await api.createPromo({ ...form, discount: +form.discount || 0 });
-      } else {
-        await api.updatePromo(modal.id, { ...form, discount: +form.discount || 0 });
-      }
+      if (modal === "new") await api.createPromo({ ...form, discount: +form.discount || 0 });
+      else await api.updatePromo(modal.id, { ...form, discount: +form.discount || 0 });
       onChanged(); setModal(null);
     } catch (e) { setError(e.message); }
   };
@@ -439,25 +556,25 @@ function PromosTab({ promos, onChanged }) {
     <div className="fade-in">
       <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}>
         <div style={{ fontSize: 13, fontWeight: 500 }}>{promos.length} promociones</div>
-        <Btn variant="primary" size="sm" onClick={() => open()}>+ Nueva</Btn>
+        <Btn variant="primary" size="sm" onClick={() => open()}><Icon name="plus" size={13} /> Nueva</Btn>
       </div>
       {promos.map(p => (
         <Card key={p.id} style={{ marginBottom: 10 }}>
           <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
-            <span style={{ fontSize: 22 }}>{p.emoji}</span>
+            <Icon name="tag" size={20} color={C.amber} />
             <div style={{ flex: 1 }}>
               <div style={{ fontWeight: 500, fontSize: 13 }}>{p.name}</div>
               <div style={{ fontSize: 11, color: C.muted }}>{p.desc}</div>
               <div style={{ fontSize: 11, color: C.amber, marginTop: 2 }}>
-                {p.type === "2x1" ? "2×1" : `${p.discount}% descuento`}
+                {p.type === "2x1" ? "2x1" : `${p.discount}% descuento`}
               </div>
             </div>
             <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
               <Badge color={p.active ? C.neon : C.muted}>{p.active ? "Activa" : "Inactiva"}</Badge>
               <Btn size="sm" variant="ghost" onClick={async () => { await api.togglePromo(p.id); onChanged(); }}>
-                {p.active ? "⏸" : "▶️"}
+                <Icon name={p.active ? "pause" : "play"} size={13} />
               </Btn>
-              <Btn size="sm" variant="ghost" onClick={() => open(p)}>✏️</Btn>
+              <Btn size="sm" variant="ghost" onClick={() => open(p)}><Icon name="edit" size={13} /></Btn>
             </div>
           </div>
         </Card>
@@ -466,9 +583,8 @@ function PromosTab({ promos, onChanged }) {
       {modal && (
         <Modal title={modal === "new" ? "Nueva promo" : "Editar promo"} onClose={() => setModal(null)}>
           <ErrorBanner message={error} />
-          <Input label="Emoji" value={form.emoji} onChange={e => setForm(f => ({ ...f, emoji: e.target.value }))} />
           <Input label="Nombre" value={form.name} onChange={e => setForm(f => ({ ...f, name: e.target.value }))} />
-          <Input label="Descripción (opcional)" value={form.desc} onChange={e => setForm(f => ({ ...f, desc: e.target.value }))} />
+          <Input label="Descripcion (opcional)" value={form.desc} onChange={e => setForm(f => ({ ...f, desc: e.target.value }))} />
           <div style={{ marginBottom: 12 }}>
             <div style={{ fontSize: 11, color: C.muted, marginBottom: 4, textTransform: "uppercase" }}>Tipo</div>
             <div style={{ display: "flex", gap: 8 }}>
@@ -477,7 +593,7 @@ function PromosTab({ promos, onChanged }) {
                   flex: 1, padding: "7px", borderRadius: 8, border: `1px solid ${form.type === t ? C.neon : C.border}`,
                   background: form.type === t ? C.neon + "22" : "transparent", color: form.type === t ? C.neon : C.muted,
                   cursor: "pointer", fontSize: 12, fontFamily: "inherit",
-                }}>{t === "percent" ? "% Descuento" : "2×1"}</button>
+                }}>{t === "percent" ? "% Descuento" : "2x1"}</button>
               ))}
             </div>
           </div>
@@ -509,25 +625,50 @@ function InventoryTab({ products, onChanged }) {
     try { await api.setStock(id, n); onChanged(); setEditing(null); } catch (e) { alert(e.message); }
   };
 
+  const toggleUnlimited = async (p) => {
+    try {
+      await api.updateProduct(p.id, {
+        name: p.name, cat: p.cat, price: p.price, stock: p.stock,
+        unlimited_stock: p.unlimited_stock ? "false" : "true",
+      });
+      onChanged();
+    } catch (e) { alert(e.message); }
+  };
+
   return (
     <div className="fade-in">
       {products.map(p => (
-        <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 10, padding: "9px 0", borderBottom: `1px solid ${C.border}` }}>
-          <span style={{ fontSize: 18 }}>{p.emoji}</span>
-          <span style={{ flex: 1, fontSize: 13 }}>{p.name}</span>
-          {editing === p.id ? (
+        <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 8, padding: "9px 0", borderBottom: `1px solid ${C.border}` }}>
+          <div style={{ width: 28, height: 28, borderRadius: 6, background: C.bg4, display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
+            <Icon name="bar" size={15} color={C.muted} />
+          </div>
+          <span style={{ flex: 1, fontSize: 13, lineHeight: 1.3 }}>{p.name}</span>
+
+          {/* Toggle ilimitado */}
+          <button onClick={() => toggleUnlimited(p)} title={p.unlimited_stock ? "Click para poner cantidad fija" : "Click para stock ilimitado"} style={{
+            background: p.unlimited_stock ? C.neon2 + "22" : C.bg4,
+            border: `1px solid ${p.unlimited_stock ? C.neon2 : C.border}`,
+            borderRadius: 6, padding: "3px 7px", cursor: "pointer", color: p.unlimited_stock ? C.neon2 : C.muted,
+            display: "flex", alignItems: "center", gap: 4, fontSize: 11,
+          }}>
+            <Icon name="infinity" size={12} />
+          </button>
+
+          {p.unlimited_stock ? (
+            <Badge color={C.neon2}>Sin limite</Badge>
+          ) : editing === p.id ? (
             <div style={{ display: "flex", gap: 4, alignItems: "center" }}>
               <input type="number" value={val} onChange={e => setVal(e.target.value)} autoFocus
-                style={{ width: 60, background: C.bg4, border: `1px solid ${C.neon}`, borderRadius: 6, color: C.text, padding: "3px 6px", fontSize: 12 }} />
-              <Btn size="sm" variant="primary" onClick={() => setStock(p.id)}>✓</Btn>
-              <Btn size="sm" variant="ghost" onClick={() => setEditing(null)}>✕</Btn>
+                style={{ width: 58, background: C.bg4, border: `1px solid ${C.neon}`, borderRadius: 6, color: C.text, padding: "3px 6px", fontSize: 12 }} />
+              <Btn size="sm" variant="primary" onClick={() => setStock(p.id)}><Icon name="check" size={12} /></Btn>
+              <Btn size="sm" variant="ghost" onClick={() => setEditing(null)}><Icon name="x" size={12} /></Btn>
             </div>
           ) : (
-            <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
+            <div style={{ display: "flex", alignItems: "center", gap: 5 }}>
               <Badge color={p.stock === 0 ? C.red : p.stock < 5 ? C.amber : C.muted}>{p.stock}</Badge>
-              <Btn size="sm" variant="ghost" onClick={() => adjust(p.id, -1)}>−</Btn>
-              <Btn size="sm" variant="ghost" onClick={() => adjust(p.id, +1)}>+</Btn>
-              <Btn size="sm" variant="ghost" onClick={() => { setEditing(p.id); setVal(p.stock); }}>✏️</Btn>
+              <Btn size="sm" variant="ghost" onClick={() => adjust(p.id, -1)}><Icon name="minus" size={12} /></Btn>
+              <Btn size="sm" variant="ghost" onClick={() => adjust(p.id, +1)}><Icon name="plus" size={12} /></Btn>
+              <Btn size="sm" variant="ghost" onClick={() => { setEditing(p.id); setVal(p.stock); }}><Icon name="edit" size={12} /></Btn>
             </div>
           )}
         </div>
@@ -535,7 +676,32 @@ function InventoryTab({ products, onChanged }) {
     </div>
   );
 }
-/* ─── Usuarios (meseros, barman, otros admins del negocio) ─── */
+
+/* ─── Configuracion de mesas y bancos ─── */
+function ConfigTab({ config, onSave }) {
+  const [mesas, setMesas] = useState(config.mesas);
+  const [bancos, setBancos] = useState(config.bancos);
+
+  const save = () => {
+    if (mesas < 1 || bancos < 0) return;
+    onSave({ mesas: Number(mesas), bancos: Number(bancos) });
+    alert("Configuracion guardada. Los cambios se reflejan al recargar la vista de mesero.");
+  };
+
+  return (
+    <div className="fade-in" style={{ maxWidth: 360 }}>
+      <div style={{ fontSize: 13, fontWeight: 500, marginBottom: 14 }}>Configuracion del local</div>
+      <Input label="Numero de mesas" type="number" min="1" max="60" value={mesas} onChange={e => setMesas(e.target.value)} />
+      <Input label="Numero de bancos en barra" type="number" min="0" max="20" value={bancos} onChange={e => setBancos(e.target.value)} />
+      <div style={{ fontSize: 11, color: C.muted, marginBottom: 14 }}>
+        Los bancos de barra apareceran como B1, B2... en el panel del barman y el mesero.
+      </div>
+      <Btn variant="primary" onClick={save}><Icon name="check" size={14} /> Guardar configuracion</Btn>
+    </div>
+  );
+}
+
+/* ─── Usuarios ─── */
 const ROLE_LABEL = { mesero: "Mesero", barman: "Barman", admin: "Admin" };
 const ROLE_COLOR = { mesero: C.blue, barman: C.neon2, admin: C.amber };
 
@@ -552,32 +718,20 @@ function UsersTab({ businessId }) {
   };
   useEffect(load, [businessId]);
 
-  const openNew = () => {
-    setForm({ name: "", role: "mesero", pin: "" });
-    setError(""); setModal(true);
-  };
+  const openNew = () => { setForm({ name: "", role: "mesero", pin: "" }); setError(""); setModal(true); };
 
   const save = async () => {
     if (!form.name.trim()) { setError("El nombre es obligatorio"); return; }
-    if (!/^\d{4,6}$/.test(form.pin)) { setError("El PIN debe ser numérico, de 4 a 6 dígitos"); return; }
+    if (!/^\d{4,6}$/.test(form.pin)) { setError("El PIN debe ser numerico, de 4 a 6 digitos"); return; }
     setSaving(true); setError("");
-    try {
-      await api.createBusinessUser(businessId, form);
-      setModal(false);
-      load();
-    } catch (e) {
-      setError(e.message);
-    } finally {
-      setSaving(false);
-    }
+    try { await api.createBusinessUser(businessId, form); setModal(false); load(); }
+    catch (e) { setError(e.message); }
+    finally { setSaving(false); }
   };
 
   const remove = async (u) => {
-    if (!confirm(`¿Eliminar a ${u.name}? Ya no podrá iniciar sesión.`)) return;
-    try {
-      await api.deleteBusinessUser(businessId, u.id);
-      load();
-    } catch (e) { alert(e.message); }
+    if (!confirm(`Eliminar a ${u.name}?`)) return;
+    try { await api.deleteBusinessUser(businessId, u.id); load(); } catch (e) { alert(e.message); }
   };
 
   if (loading) return <div style={{ textAlign: "center", color: C.muted, marginTop: 40 }}>Cargando...</div>;
@@ -585,27 +739,25 @@ function UsersTab({ businessId }) {
   return (
     <div className="fade-in">
       <div style={{ fontSize: 11, color: C.muted, marginBottom: 12 }}>
-        Cada mesero/barman con su propio PIN queda registrado en cada orden y pago que haga —
-        así sabes quién atendió cada mesa y quién cobró cada cuenta.
+        Cada mesero/barman con su propio PIN queda registrado en cada orden y pago que haga.
       </div>
       <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}>
         <div style={{ fontSize: 13, fontWeight: 500 }}>{users.length} usuarios</div>
-        <Btn variant="primary" size="sm" onClick={openNew}>+ Nuevo usuario</Btn>
+        <Btn variant="primary" size="sm" onClick={openNew}><Icon name="plus" size={13} /> Nuevo usuario</Btn>
       </div>
-
       {users.map(u => (
         <Card key={u.id} style={{ marginBottom: 8 }}>
           <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
+            <Icon name="users" size={18} color={ROLE_COLOR[u.role] || C.muted} />
             <div style={{ flex: 1 }}>
               <div style={{ fontWeight: 500, fontSize: 13 }}>{u.name}</div>
               <div style={{ fontSize: 11, color: C.muted }}>PIN: {u.pin}</div>
             </div>
             <Badge color={ROLE_COLOR[u.role] || C.muted}>{ROLE_LABEL[u.role] || u.role}</Badge>
-            <Btn size="sm" variant="ghost" onClick={() => remove(u)}>🗑</Btn>
+            <Btn size="sm" variant="ghost" onClick={() => remove(u)}><Icon name="trash" size={13} /></Btn>
           </div>
         </Card>
       ))}
-
       {modal && (
         <Modal title="Nuevo usuario" onClose={() => setModal(false)}>
           <ErrorBanner message={error} />
@@ -622,13 +774,9 @@ function UsersTab({ businessId }) {
               ))}
             </div>
           </div>
-          <Input
-            label="PIN (4-6 dígitos, único en este negocio)"
-            value={form.pin}
+          <Input label="PIN (4-6 digitos, unico en este negocio)" value={form.pin}
             onChange={e => setForm(f => ({ ...f, pin: e.target.value.replace(/\D/g, "") }))}
-            placeholder="ej. 4521"
-            maxLength={6}
-          />
+            placeholder="ej. 4521" maxLength={6} />
           <div style={{ display: "flex", gap: 8, marginTop: 4 }}>
             <Btn variant="ghost" onClick={() => setModal(false)} style={{ flex: 1 }}>Cancelar</Btn>
             <Btn variant="primary" onClick={save} disabled={saving} style={{ flex: 1 }}>
diff --git a/frontend/src/components/BarmanView.jsx b/frontend/src/components/BarmanView.jsx
index baf204b..1f39625 100644
--- a/frontend/src/components/BarmanView.jsx
+++ b/frontend/src/components/BarmanView.jsx
@@ -1,24 +1,30 @@
 import { useState } from "react";
 import { C, fmt, today } from "../styles.js";
 import { api } from "../api.js";
-import { Btn, Badge, Modal } from "./Common.jsx";
+import { Btn, Badge, Modal, SearchInput, Icon } from "./Common.jsx";
+import { PayModal } from "./AdminView.jsx";
 
 const API_URL = import.meta.env.VITE_API_URL || "http://localhost:4000";
 
-const STATUS_FLOW = ["pendiente", "preparando", "listo"];
+const STATUS_FLOW  = ["pendiente", "preparando", "listo"];
 const STATUS_LABEL = { pendiente: "Pendiente", preparando: "Preparando", listo: "Listo" };
 const STATUS_COLOR = { pendiente: C.red, preparando: C.amber, listo: C.neon };
-const BARRAS = ["B1", "B2", "B3", "B4"];
 
-// Aplana todas las órdenes activas en una lista de "tickets de producto"
-// (un item = una fila), que es como un barman real trabaja: viendo qué
-// falta preparar, no viendo "órdenes" completas como cajas cerradas.
+function loadVenueConfig(businessId) {
+  try { return JSON.parse(localStorage.getItem(`bar_config_${businessId}`)) || { mesas: 10, bancos: 4 }; }
+  catch { return { mesas: 10, bancos: 4 }; }
+}
+
+function isBarra(mesa) {
+  return /^B\d+$/.test(String(mesa));
+}
+
 function flattenItems(orders) {
   const rows = [];
   for (const order of orders) {
     if (order.is_closed) continue;
     order.items.forEach((item, idx) => {
-      if (item.paid) return; // ya cobrado y servido, no debe seguir en cola
+      if (item.paid) return;
       rows.push({ orderId: order.id, mesa: order.mesa, note: order.note, itemIndex: idx, item });
     });
   }
@@ -26,8 +32,12 @@ function flattenItems(orders) {
 }
 
 export default function BarmanView({ user, products, orders, onOrdersChanged, onProductsChanged, onLogout }) {
-  const [filter, setFilter] = useState("todas"); // todas | pendiente | preparando | listo
+  const cfg = loadVenueConfig(user.businessId);
+  const BARRAS = Array.from({ length: cfg.bancos }, (_, i) => `B${i + 1}`);
+
+  const [filter, setFilter] = useState("todas");
   const [barraModal, setBarraModal] = useState(false);
+  const [cobrarOrder, setCobrarOrder] = useState(null);
 
   const rows = flattenItems(orders);
   const filtered = filter === "todas" ? rows : rows.filter(r => r.item.status === filter);
@@ -42,32 +52,58 @@ export default function BarmanView({ user, products, orders, onOrdersChanged, on
     const idx = STATUS_FLOW.indexOf(row.item.status);
     const next = STATUS_FLOW[idx + 1];
     if (!next) return;
-    try {
-      await api.updateItemStatus(row.orderId, row.itemIndex, next);
-      onOrdersChanged();
-    } catch (e) {
-      alert("Error: " + e.message);
-    }
+    try { await api.updateItemStatus(row.orderId, row.itemIndex, next); onOrdersChanged(); }
+    catch (e) { alert("Error: " + e.message); }
   };
 
+  // Cuentas de barra abiertas que el barman puede cobrar
+  const barraCuentasAbiertas = orders.filter(o => !o.is_closed && isBarra(o.mesa));
+
   return (
     <div style={{ display: "flex", flexDirection: "column", height: "100vh", background: C.bg }}>
 
       <div style={{ background: C.bg2, borderBottom: `1px solid ${C.border}`, padding: "10px 14px", display: "flex", justifyContent: "space-between", alignItems: "center" }}>
         <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
-          <span style={{ fontSize: 20 }}>🍹</span>
+          <Icon name="bar" size={20} color={C.neon2} />
           <div>
             <div style={{ fontWeight: 600, color: C.neon2, fontSize: 13 }}>{user.name?.toUpperCase() || "BARMAN"}</div>
             <div style={{ fontSize: 11, color: C.muted }}>{today()}</div>
           </div>
         </div>
         <div style={{ display: "flex", gap: 8 }}>
-          <Btn size="sm" variant="purple" onClick={() => setBarraModal(true)}>+ Consumo en barra</Btn>
-          <Btn size="sm" variant="ghost" onClick={onLogout}>Salir</Btn>
+          {BARRAS.length > 0 && (
+            <Btn size="sm" variant="purple" onClick={() => setBarraModal(true)}>
+              <Icon name="plus" size={13} /> Consumo en barra
+            </Btn>
+          )}
+          <Btn size="sm" variant="ghost" onClick={onLogout}><Icon name="logout" size={13} /> Salir</Btn>
         </div>
       </div>
 
-      {/* Filtros por estado */}
+      {/* Cuentas de barra activas — el barman puede cobrarlas directamente */}
+      {barraCuentasAbiertas.length > 0 && (
+        <div style={{ background: C.bg3, borderBottom: `1px solid ${C.border}`, padding: "8px 12px" }}>
+          <div style={{ fontSize: 11, color: C.muted, marginBottom: 6, textTransform: "uppercase", letterSpacing: 1 }}>
+            Cuentas de barra abiertas
+          </div>
+          <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
+            {barraCuentasAbiertas.map(o => (
+              <button key={o.id} onClick={() => setCobrarOrder(o)} style={{
+                background: C.bg4, border: `1px solid ${C.neon2}`, borderRadius: 8,
+                padding: "5px 10px", cursor: "pointer", color: C.text, fontFamily: "inherit",
+                display: "flex", alignItems: "center", gap: 6, fontSize: 12,
+              }}>
+                <Icon name="bar" size={12} color={C.neon2} />
+                Barra {o.mesa}
+                <span style={{ color: C.neon, fontWeight: 600 }}>{fmt(o.total)}</span>
+                <span style={{ background: C.amber, color: "#000", borderRadius: 4, padding: "1px 5px", fontSize: 10, fontWeight: 600 }}>Cobrar</span>
+              </button>
+            ))}
+          </div>
+        </div>
+      )}
+
+      {/* Filtros */}
       <div style={{ display: "flex", gap: 6, padding: "8px 12px", background: C.bg3, borderBottom: `1px solid ${C.border}` }}>
         {["todas", "pendiente", "preparando", "listo"].map(f => (
           <button key={f} onClick={() => setFilter(f)} style={{
@@ -82,15 +118,15 @@ export default function BarmanView({ user, products, orders, onOrdersChanged, on
         ))}
       </div>
 
-      {/* Lista de productos por preparar, no de órdenes completas */}
+      {/* Cola de productos */}
       <div style={{ flex: 1, overflowY: "auto", padding: 12 }}>
         {!filtered.length && (
           <div style={{ textAlign: "center", color: C.muted, marginTop: 60, fontSize: 13 }}>
-            🎉 Sin productos pendientes
+            Sin productos pendientes
           </div>
         )}
         <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(220px, 1fr))", gap: 10 }}>
-          {filtered.map((row, i) => (
+          {filtered.map((row) => (
             <div key={`${row.orderId}-${row.itemIndex}`} style={{
               background: C.bg2, border: `1px solid ${STATUS_COLOR[row.item.status]}55`,
               borderRadius: 12, padding: "12px 14px",
@@ -107,17 +143,19 @@ export default function BarmanView({ user, products, orders, onOrdersChanged, on
                   <img src={`${API_URL}${row.item.image_url}`} alt={row.item.name}
                     style={{ width: 40, height: 40, borderRadius: 8, objectFit: "cover" }} />
                 ) : (
-                  <span style={{ fontSize: 26 }}>{row.item.emoji}</span>
+                  <div style={{ width: 40, height: 40, borderRadius: 8, background: C.bg4, display: "flex", alignItems: "center", justifyContent: "center" }}>
+                    <Icon name="bar" size={22} color={C.muted} />
+                  </div>
                 )}
                 <div>
-                  <div style={{ fontWeight: 500, fontSize: 13 }}>{row.item.qty}× {row.item.name}</div>
+                  <div style={{ fontWeight: 500, fontSize: 13 }}>{row.item.qty}x {row.item.name}</div>
                   <div style={{ fontSize: 11, color: C.muted }}>{fmt(row.item.price * row.item.qty)}</div>
                 </div>
               </div>
 
               {row.note && (
-                <div style={{ background: C.amber + "15", border: `1px solid ${C.amber}44`, borderRadius: 6, padding: "5px 8px", fontSize: 11, color: C.amber, marginBottom: 8 }}>
-                  ⚠ {row.note}
+                <div style={{ background: C.amber + "15", border: `1px solid ${C.amber}44`, borderRadius: 6, padding: "5px 8px", fontSize: 11, color: C.amber, marginBottom: 8, display: "flex", alignItems: "center", gap: 5 }}>
+                  <Icon name="warning" size={12} color={C.amber} /> {row.note}
                 </div>
               )}
 
@@ -131,8 +169,8 @@ export default function BarmanView({ user, products, orders, onOrdersChanged, on
                   {row.item.status === "pendiente" ? "Empezar a preparar" : "Marcar listo"}
                 </Btn>
               ) : (
-                <div style={{ textAlign: "center", fontSize: 11, color: C.neon }}>
-                  ✓ Listo para servir — pendiente de cobro
+                <div style={{ textAlign: "center", fontSize: 11, color: C.neon, display: "flex", alignItems: "center", justifyContent: "center", gap: 4 }}>
+                  <Icon name="check" size={13} color={C.neon} /> Listo para servir
                 </div>
               )}
             </div>
@@ -143,28 +181,39 @@ export default function BarmanView({ user, products, orders, onOrdersChanged, on
       {barraModal && (
         <BarraOrderModal
           products={products}
+          barras={BARRAS}
           onClose={() => setBarraModal(false)}
           onSent={() => { setBarraModal(false); onOrdersChanged(); }}
         />
       )}
+
+      {cobrarOrder && (
+        <PayModal
+          order={cobrarOrder}
+          onClose={() => setCobrarOrder(null)}
+          onPaid={() => { setCobrarOrder(null); onOrdersChanged(); }}
+        />
+      )}
     </div>
   );
 }
 
-function isBarra(mesa) {
-  return BARRAS.includes(String(mesa));
-}
-
-// El barman registra consumo de alguien sentado directamente en la barra,
-// usando exactamente el mismo flujo que un mesero: elige "mesa" de barra,
-// agrega productos, envía. Esa cuenta luego se cobra igual que cualquier
-// otra (desde el propio panel del barman, ya que él la atendió).
-function BarraOrderModal({ products, onClose, onSent }) {
-  const [barra, setBarra] = useState(BARRAS[0]);
+function BarraOrderModal({ products, barras, onClose, onSent }) {
+  const [barra, setBarra] = useState(barras[0] || "B1");
+  const [cat, setCat] = useState(null);
+  const [search, setSearch] = useState("");
   const [cart, setCart] = useState({});
   const [sending, setSending] = useState(false);
   const [error, setError] = useState("");
 
+  const cats = [...new Set(products.map(p => p.cat))];
+  const activeCat = cat || cats[0];
+
+  const visibleProducts = products.filter(p => {
+    if (search) return p.name.toLowerCase().includes(search.toLowerCase());
+    return p.cat === activeCat;
+  });
+
   const cartItems = Object.values(cart);
   const total = cartItems.reduce((s, x) => s + x.price * x.qty, 0);
 
@@ -188,26 +237,22 @@ function BarraOrderModal({ products, onClose, onSent }) {
   const send = async () => {
     if (!cartItems.length) { setError("Agrega al menos un producto"); return; }
     setSending(true); setError("");
-    try {
-      await api.createOrder({ mesa: barra, items: cartItems, note: "" });
-      onSent();
-    } catch (e) {
-      setError(e.message);
-    } finally {
-      setSending(false);
-    }
+    try { await api.createOrder({ mesa: barra, items: cartItems, note: "" }); onSent(); }
+    catch (e) { setError(e.message); }
+    finally { setSending(false); }
   };
 
   return (
-    <Modal title="Consumo en barra" onClose={onClose}>
+    <Modal title="Consumo en barra" onClose={onClose} maxWidth={500}>
       {error && (
         <div style={{ background: C.red + "22", border: `1px solid ${C.red}`, color: C.red, borderRadius: 8, padding: "8px 12px", fontSize: 12, marginBottom: 10 }}>
           {error}
         </div>
       )}
 
-      <div style={{ display: "flex", gap: 6, marginBottom: 12 }}>
-        {BARRAS.map(b => (
+      {/* Selector de banco */}
+      <div style={{ display: "flex", gap: 6, marginBottom: 10 }}>
+        {barras.map(b => (
           <button key={b} onClick={() => setBarra(b)} style={{
             flex: 1, padding: "6px 0", borderRadius: 8, fontSize: 12,
             background: barra === b ? C.neon2 + "22" : "transparent",
@@ -217,36 +262,72 @@ function BarraOrderModal({ products, onClose, onSent }) {
         ))}
       </div>
 
-      <div style={{ maxHeight: 240, overflowY: "auto", display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 8, marginBottom: 12 }}>
-        {products.map(p => {
+      {/* Búsqueda */}
+      <SearchInput value={search} onChange={setSearch} placeholder="Buscar producto..." style={{ marginBottom: 8 }} />
+
+      {/* Categorías (cuando no hay búsqueda activa) */}
+      {!search && (
+        <div style={{ display: "flex", gap: 6, overflowX: "auto", marginBottom: 10 }}>
+          {cats.map(c => (
+            <button key={c} onClick={() => setCat(c)} style={{
+              padding: "4px 12px", borderRadius: 16, whiteSpace: "nowrap", fontSize: 11,
+              background: activeCat === c ? C.neon2 + "22" : "transparent",
+              border: `1px solid ${activeCat === c ? C.neon2 : C.border}`,
+              color: activeCat === c ? C.neon2 : C.muted, cursor: "pointer",
+            }}>{c}</button>
+          ))}
+        </div>
+      )}
+
+      {/* Grilla de productos */}
+      <div style={{ maxHeight: 220, overflowY: "auto", display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(100px, 1fr))", gap: 8, marginBottom: 12 }}>
+        {visibleProducts.map(p => {
           const agotado = p.stock === 0 && !p.unlimited_stock;
+          const inCart = cart[p.id];
           return (
             <div key={p.id} onClick={() => addItem(p)} style={{
-              background: C.bg3, border: `1px solid ${C.border}`, borderRadius: 8,
+              background: C.bg3, border: `1px solid ${inCart ? C.neon + "66" : C.border}`, borderRadius: 8,
               padding: "8px 6px", textAlign: "center", cursor: agotado ? "not-allowed" : "pointer",
-              opacity: agotado ? 0.4 : 1,
+              opacity: agotado ? 0.4 : 1, position: "relative",
             }}>
-              <div style={{ fontSize: 20 }}>{p.emoji}</div>
-              <div style={{ fontSize: 10, marginTop: 2 }}>{p.name}</div>
+              {p.image_url ? (
+                <img src={`${API_URL}${p.image_url}`} alt={p.name} style={{ width: "100%", height: 50, objectFit: "cover", borderRadius: 6, marginBottom: 4 }} />
+              ) : (
+                <div style={{ height: 40, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 4 }}>
+                  <Icon name="bar" size={22} color={C.muted} />
+                </div>
+              )}
+              <div style={{ fontSize: 10, marginTop: 2, lineHeight: 1.2 }}>{p.name}</div>
               <div style={{ fontSize: 11, color: C.neon }}>{fmt(p.price)}</div>
-              {cart[p.id] && <div style={{ fontSize: 10, color: C.amber }}>×{cart[p.id].qty}</div>}
+              {inCart && (
+                <div style={{
+                  position: "absolute", top: 4, right: 4,
+                  background: C.neon, color: "#000", borderRadius: "50%",
+                  width: 18, height: 18, fontSize: 10, fontWeight: 700,
+                  display: "flex", alignItems: "center", justifyContent: "center",
+                }}>{inCart.qty}</div>
+              )}
             </div>
           );
         })}
+        {visibleProducts.length === 0 && (
+          <div style={{ gridColumn: "1/-1", textAlign: "center", color: C.muted, padding: "20px 0", fontSize: 12 }}>Sin resultados</div>
+        )}
       </div>
 
+      {/* Resumen del carrito */}
       {cartItems.length > 0 && (
-        <div style={{ marginBottom: 10 }}>
+        <div style={{ marginBottom: 10, background: C.bg3, borderRadius: 8, padding: "8px 10px" }}>
           {cartItems.map(item => (
             <div key={item.id} style={{ display: "flex", justifyContent: "space-between", fontSize: 12, padding: "3px 0" }}>
-              <span>{item.qty}× {item.name}</span>
+              <span>{item.qty}x {item.name}</span>
               <div style={{ display: "flex", gap: 6, alignItems: "center" }}>
                 <button onClick={() => removeItem(item.id)} style={{ width: 18, height: 18, borderRadius: "50%", border: `1px solid ${C.border2}`, background: C.bg4, color: C.text, fontSize: 12, cursor: "pointer" }}>−</button>
                 <span>{fmt(item.price * item.qty)}</span>
               </div>
             </div>
           ))}
-          <div style={{ display: "flex", justifyContent: "space-between", fontWeight: 600, marginTop: 6, fontSize: 13 }}>
+          <div style={{ display: "flex", justifyContent: "space-between", fontWeight: 600, marginTop: 6, fontSize: 13, borderTop: `1px solid ${C.border}`, paddingTop: 6 }}>
             <span>Total</span><span style={{ color: C.neon }}>{fmt(total)}</span>
           </div>
         </div>
diff --git a/frontend/src/components/Common.jsx b/frontend/src/components/Common.jsx
index bb776b4..2a1f149 100644
--- a/frontend/src/components/Common.jsx
+++ b/frontend/src/components/Common.jsx
@@ -37,13 +37,15 @@ export const Card = ({ children, style = {}, onClick }) => (
   </div>
 );
 
-export const Modal = ({ children, onClose, title }) => (
+export const Modal = ({ children, onClose, title, maxWidth = 420 }) => (
   <div style={{ position: "fixed", inset: 0, background: "#000b", zIndex: 100, display: "flex", alignItems: "center", justifyContent: "center", padding: 16 }} onClick={onClose}>
-    <div onClick={e => e.stopPropagation()} className="fade-in" style={{ background: C.bg2, border: `1px solid ${C.border2}`, borderRadius: 16, padding: 20, width: "100%", maxWidth: 420, maxHeight: "90vh", overflowY: "auto" }}>
+    <div onClick={e => e.stopPropagation()} className="fade-in" style={{ background: C.bg2, border: `1px solid ${C.border2}`, borderRadius: 16, padding: 20, width: "100%", maxWidth, maxHeight: "90vh", overflowY: "auto" }}>
       {title && (
         <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}>
           <span style={{ fontSize: 16, fontWeight: 600, color: C.neon }}>{title}</span>
-          <Btn size="sm" variant="ghost" onClick={onClose}>✕</Btn>
+          <Btn size="sm" variant="ghost" onClick={onClose}>
+            <Icon name="x" size={14} />
+          </Btn>
         </div>
       )}
       {children}
@@ -64,4 +66,68 @@ export const ErrorBanner = ({ message }) => !message ? null : (
   <div style={{ background: C.red + "22", border: `1px solid ${C.red}`, color: C.red, borderRadius: 8, padding: "8px 12px", fontSize: 12, marginBottom: 10 }}>
     {message}
   </div>
-);
\ No newline at end of file
+);
+
+// Buscador reutilizable
+export const SearchInput = ({ value, onChange, placeholder = "Buscar...", style = {} }) => (
+  <div style={{ position: "relative", ...style }}>
+    <span style={{ position: "absolute", left: 8, top: "50%", transform: "translateY(-50%)", pointerEvents: "none" }}>
+      <Icon name="search" size={14} color={C.muted} />
+    </span>
+    <input
+      value={value}
+      onChange={e => onChange(e.target.value)}
+      placeholder={placeholder}
+      style={{
+        width: "100%", background: C.bg3, border: `1px solid ${C.border2}`,
+        borderRadius: 8, color: C.text, padding: "7px 10px 7px 30px",
+        fontSize: 12, outline: "none",
+      }}
+    />
+    {value && (
+      <button onClick={() => onChange("")} style={{
+        position: "absolute", right: 6, top: "50%", transform: "translateY(-50%)",
+        background: "none", border: "none", color: C.muted, cursor: "pointer", padding: 2,
+      }}>
+        <Icon name="x" size={12} />
+      </button>
+    )}
+  </div>
+);
+
+// Íconos SVG inline — sin emojis, sin librería externa
+const ICONS = {
+  x: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><line x1="2" y1="2" x2="14" y2="14"/><line x1="14" y1="2" x2="2" y2="14"/></svg>,
+  search: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><circle cx="6.5" cy="6.5" r="4.5"/><line x1="10" y1="10" x2="14" y2="14"/></svg>,
+  edit: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M11 2l3 3-8 8H3v-3l8-8z"/></svg>,
+  trash: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><polyline points="2,4 14,4"/><path d="M5 4V2h6v2"/><path d="M3 4l1 10h8l1-10"/></svg>,
+  check: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"><polyline points="2,8 6,12 14,4"/></svg>,
+  plus: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><line x1="8" y1="2" x2="8" y2="14"/><line x1="2" y1="8" x2="14" y2="8"/></svg>,
+  minus: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><line x1="2" y1="8" x2="14" y2="8"/></svg>,
+  receipt: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M3 2h10v12l-2-1.5-2 1.5-2-1.5L5 14 3 14V2z"/><line x1="5" y1="6" x2="11" y2="6"/><line x1="5" y1="9" x2="9" y2="9"/></svg>,
+  card: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><rect x="1" y="3" width="14" height="10" rx="2"/><line x1="1" y1="7" x2="15" y2="7"/></svg>,
+  cash: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><rect x="1" y="4" width="14" height="8" rx="1.5"/><circle cx="8" cy="8" r="2"/><line x1="4" y1="4" x2="4" y2="12"/><line x1="12" y1="4" x2="12" y2="12"/></svg>,
+  qr: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><rect x="2" y="2" width="5" height="5" rx="1"/><rect x="9" y="2" width="5" height="5" rx="1"/><rect x="2" y="9" width="5" height="5" rx="1"/><rect x="10" y="10" width="2" height="2"/><line x1="12" y1="9" x2="14" y2="9"/><line x1="14" y1="11" x2="14" y2="14"/><line x1="12" y1="14" x2="14" y2="14"/></svg>,
+  bar: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M3 2h10l-3 5H6L3 2z"/><line x1="8" y1="7" x2="8" y2="13"/><line x1="5" y1="13" x2="11" y2="13"/></svg>,
+  admin: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><rect x="2" y="2" width="5" height="5" rx="1"/><rect x="9" y="2" width="5" height="5" rx="1"/><rect x="2" y="9" width="5" height="5" rx="1"/><rect x="9" y="9" width="5" height="5" rx="1"/></svg>,
+  menu: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><line x1="2" y1="5" x2="14" y2="5"/><line x1="2" y1="8" x2="14" y2="8"/><line x1="2" y1="11" x2="14" y2="11"/></svg>,
+  users: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><circle cx="6" cy="6" r="3"/><path d="M1 14c0-3 2-5 5-5s5 2 5 5"/><path d="M11 3a3 3 0 010 6"/><path d="M15 14c0-3-1.5-4.5-4-5"/></svg>,
+  tag: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M2 2h5l7 7-5 5-7-7V2z"/><circle cx="5" cy="5" r="1" fill="currentColor" stroke="none"/></svg>,
+  box: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M2 5l6-3 6 3v6l-6 3-6-3V5z"/><line x1="8" y1="2" x2="8" y2="14"/><path d="M2 5l6 3 6-3"/></svg>,
+  table: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><rect x="1" y="4" width="14" height="3" rx="1"/><line x1="4" y1="7" x2="4" y2="14"/><line x1="12" y1="7" x2="12" y2="14"/></svg>,
+  logout: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M6 2H3a1 1 0 00-1 1v10a1 1 0 001 1h3"/><polyline points="10,10 14,8 10,6"/><line x1="14" y1="8" x2="5" y2="8"/></svg>,
+  pause: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><line x1="5" y1="3" x2="5" y2="13"/><line x1="11" y1="3" x2="11" y2="13"/></svg>,
+  play: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><polygon points="3,2 13,8 3,14"/></svg>,
+  camera: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M1 5h14v9H1z" rx="1"/><circle cx="8" cy="9.5" r="2.5"/><path d="M5 5l1.5-2h3L11 5"/></svg>,
+  info: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><circle cx="8" cy="8" r="6"/><line x1="8" y1="7" x2="8" y2="11"/><circle cx="8" cy="5" r=".5" fill="currentColor" stroke="none"/></svg>,
+  warning: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M8 2L1 14h14L8 2z"/><line x1="8" y1="7" x2="8" y2="10"/><circle cx="8" cy="12" r=".5" fill="currentColor" stroke="none"/></svg>,
+  settings: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><circle cx="8" cy="8" r="2.5"/><path d="M8 1v2M8 13v2M1 8h2M13 8h2M3 3l1.4 1.4M11.6 11.6L13 13M3 13l1.4-1.4M11.6 4.4L13 3"/></svg>,
+  note: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M3 2h10v9l-3 3H3z"/><line x1="5" y1="6" x2="11" y2="6"/><line x1="5" y1="9" x2="8" y2="9"/></svg>,
+  infinity: (s) => <svg width={s} height={s} viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d="M8 8c-1-2-2.5-3-4-3a3 3 0 000 6c1.5 0 3-1 4-3z"/><path d="M8 8c1 2 2.5 3 4 3a3 3 0 000-6c-1.5 0-3 1-4 3z"/></svg>,
+};
+
+export const Icon = ({ name, size = 16, color = "currentColor" }) => {
+  const fn = ICONS[name];
+  if (!fn) return null;
+  return <span style={{ color, display: "inline-flex", alignItems: "center", flexShrink: 0 }}>{fn(size)}</span>;
+};
diff --git a/frontend/src/components/MeseroView.jsx b/frontend/src/components/MeseroView.jsx
index 4f47a2d..1342b24 100644
--- a/frontend/src/components/MeseroView.jsx
+++ b/frontend/src/components/MeseroView.jsx
@@ -1,24 +1,29 @@
 import { useState } from "react";
 import { C, fmt, today } from "../styles.js";
 import { api } from "../api.js";
-import { Btn, Badge, Divider, Modal } from "./Common.jsx";
+import { Btn, Badge, Divider, Modal, SearchInput, Icon } from "./Common.jsx";
+import { PayModal } from "./AdminView.jsx";
 
 const API_URL = import.meta.env.VITE_API_URL || "http://localhost:4000";
 
-const CATS_FALLBACK = ["Cervezas", "Licores", "Cócteles", "Botanas"];
-const MESAS = Array.from({ length: 10 }, (_, i) => i + 1);
-const PAY_METHODS = [
-  { id: "ef", label: "Efectivo" },
-  { id: "ta", label: "Tarjeta" },
-  { id: "qr", label: "QR/Trans." },
-];
+const CATS_FALLBACK = ["Cervezas", "Licores", "Cocteles", "Botanas"];
+
+// Lee cuántas mesas y bancos configuró el admin
+function loadVenueConfig(businessId) {
+  try { return JSON.parse(localStorage.getItem(`bar_config_${businessId}`)) || { mesas: 10, bancos: 4 }; }
+  catch { return { mesas: 10, bancos: 4 }; }
+}
 
 export default function MeseroView({ user, products, promos, orders, onOrdersChanged, onLogout }) {
+  const cfg = loadVenueConfig(user.businessId);
+  const MESAS = Array.from({ length: cfg.mesas }, (_, i) => i + 1);
+
   const [mesa, setMesa] = useState(1);
   const cats = [...new Set(products.map(p => p.cat))].length
     ? [...new Set(products.map(p => p.cat))]
     : CATS_FALLBACK;
   const [cat, setCat] = useState(cats[0]);
+  const [search, setSearch] = useState("");
   const [cart, setCart] = useState({});
   const [note, setNote] = useState("");
   const [disc, setDisc] = useState(0);
@@ -29,21 +34,17 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
   const [closeModal, setCloseModal] = useState(false);
 
   const activePromos = promos.filter(p => p.active);
-  // La cuenta activa de la mesa: lo que ya se mandó a barra, sin importar
-  // quién lo mandó ni cuántas rondas se hayan acumulado.
   const mesaOrder = orders.find(o => String(o.mesa) === String(mesa) && !o.is_closed);
   const cartItems = Object.values(cart);
   const sub = cartItems.reduce((s, x) => s + x.price * x.qty, 0);
   const promoDisc = activePromo
-    ? activePromo.type === "2x1"
-      ? sub * 0.5
-      : sub * activePromo.discount / 100
+    ? activePromo.type === "2x1" ? sub * 0.5 : sub * activePromo.discount / 100
     : 0;
   const manualDisc = sub * disc / 100;
   const total = Math.max(0, sub - promoDisc - manualDisc);
 
   const addItem = (p) => {
-    if (p.stock === 0) return;
+    if (p.stock === 0 && !p.unlimited_stock) return;
     setCart(c => ({
       ...c,
       [p.id]: c[p.id] ? { ...c[p.id], qty: c[p.id].qty + 1 } : { ...p, qty: 1 },
@@ -74,13 +75,20 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
     }
   };
 
+  // Productos filtrados por categoría y búsqueda
+  const visibleProducts = products.filter(p => {
+    const matchCat = !search && p.cat === cat;
+    const matchSearch = search && p.name.toLowerCase().includes(search.toLowerCase());
+    return search ? matchSearch : matchCat;
+  });
+
   return (
     <div style={{ display: "flex", flexDirection: "column", height: "100vh", background: C.bg }}>
 
-      {/* ── Header ── */}
+      {/* Header */}
       <div style={{ background: C.bg2, borderBottom: `1px solid ${C.border}`, padding: "10px 14px", display: "flex", justifyContent: "space-between", alignItems: "center" }}>
         <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
-          <span style={{ fontSize: 20 }}>🧑‍🍳</span>
+          <Icon name="menu" size={20} color={C.neon} />
           <div>
             <div style={{ fontWeight: 600, color: C.neon, fontSize: 13 }}>{user.name?.toUpperCase() || "MESERO"}</div>
             <div style={{ fontSize: 11, color: C.muted }}>{today()}</div>
@@ -88,11 +96,11 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
         </div>
         <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
           {mesaOrder && <Badge color={C.amber}>Mesa {mesa}: cuenta activa</Badge>}
-          <Btn size="sm" variant="ghost" onClick={onLogout}>Salir</Btn>
+          <Btn size="sm" variant="ghost" onClick={onLogout}><Icon name="logout" size={13} /> Salir</Btn>
         </div>
       </div>
 
-      {/* ── Selector de mesas ── */}
+      {/* Selector de mesas */}
       <div style={{ background: C.bg3, borderBottom: `1px solid ${C.border}`, padding: "8px 12px", overflowX: "auto" }}>
         <div style={{ display: "flex", gap: 6, minWidth: "max-content" }}>
           {MESAS.map(m => {
@@ -112,74 +120,69 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
         </div>
       </div>
 
-      {/* ── Cuerpo principal ── */}
+      {/* Cuerpo */}
       <div style={{ flex: 1, display: "flex", overflow: "hidden" }}>
 
-        {/* ── Menú ── */}
+        {/* Menu */}
         <div style={{ flex: 1, display: "flex", flexDirection: "column", overflow: "hidden" }}>
 
-          {/* Categorías + promos */}
-          <div style={{ display: "flex", gap: 6, padding: "8px 12px", overflowX: "auto", background: C.bg2, borderBottom: `1px solid ${C.border}` }}>
-            {cats.map(c => (
-              <button key={c} onClick={() => setCat(c)} style={{
-                padding: "4px 14px", borderRadius: 16, whiteSpace: "nowrap",
-                background: cat === c ? C.neon2 + "22" : "transparent",
-                border: `1px solid ${cat === c ? C.neon2 : C.border}`,
-                color: cat === c ? C.neon2 : C.muted, fontSize: 12, cursor: "pointer",
-              }}>{c}</button>
-            ))}
-            {activePromos.map(p => (
-              <button key={p.id} onClick={() => setActivePromo(activePromo?.id === p.id ? null : p)} style={{
-                padding: "4px 14px", borderRadius: 16, whiteSpace: "nowrap",
-                background: activePromo?.id === p.id ? C.amber + "22" : "transparent",
-                border: `1px solid ${activePromo?.id === p.id ? C.amber : C.border}`,
-                color: activePromo?.id === p.id ? C.amber : C.muted, fontSize: 12, cursor: "pointer",
-              }}>{p.emoji} {p.name}</button>
-            ))}
+          {/* Barra de búsqueda + categorías */}
+          <div style={{ background: C.bg2, borderBottom: `1px solid ${C.border}`, padding: "8px 12px" }}>
+            <SearchInput value={search} onChange={setSearch} placeholder="Buscar producto..." style={{ marginBottom: search ? 0 : 8 }} />
+            {!search && (
+              <div style={{ display: "flex", gap: 6, overflowX: "auto", paddingTop: 8 }}>
+                {cats.map(c => (
+                  <button key={c} onClick={() => setCat(c)} style={{
+                    padding: "4px 14px", borderRadius: 16, whiteSpace: "nowrap",
+                    background: cat === c ? C.neon2 + "22" : "transparent",
+                    border: `1px solid ${cat === c ? C.neon2 : C.border}`,
+                    color: cat === c ? C.neon2 : C.muted, fontSize: 12, cursor: "pointer",
+                  }}>{c}</button>
+                ))}
+                {activePromos.map(p => (
+                  <button key={p.id} onClick={() => setActivePromo(activePromo?.id === p.id ? null : p)} style={{
+                    padding: "4px 14px", borderRadius: 16, whiteSpace: "nowrap",
+                    background: activePromo?.id === p.id ? C.amber + "22" : "transparent",
+                    border: `1px solid ${activePromo?.id === p.id ? C.amber : C.border}`,
+                    color: activePromo?.id === p.id ? C.amber : C.muted, fontSize: 12, cursor: "pointer",
+                  }}>
+                    <Icon name="tag" size={11} /> {p.name}
+                  </button>
+                ))}
+              </div>
+            )}
           </div>
 
           {/* Grilla de productos */}
           <div style={{
             flex: 1, overflowY: "auto", padding: 12,
-            display: "grid",
-            gridTemplateColumns: "repeat(auto-fill, minmax(130px, 1fr))",
+            display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(130px, 1fr))",
             gap: 10, alignContent: "start",
           }}>
-            {products.filter(p => p.cat === cat).map(p => {
+            {visibleProducts.length === 0 && (
+              <div style={{ gridColumn: "1/-1", textAlign: "center", color: C.muted, marginTop: 30, fontSize: 12 }}>
+                Sin resultados
+              </div>
+            )}
+            {visibleProducts.map(p => {
               const inCart = cart[p.id];
               const agotado = p.stock === 0 && !p.unlimited_stock;
               return (
-                <div
-                  key={p.id}
-                  onClick={() => addItem(p)}
-                  style={{
-                    background: C.bg3,
-                    border: `1px solid ${agotado ? C.red + "44" : inCart ? C.neon + "66" : C.border}`,
-                    borderRadius: 12, overflow: "hidden",
-                    cursor: agotado ? "not-allowed" : "pointer",
-                    opacity: agotado ? 0.5 : 1,
-                    userSelect: "none",
-                    transition: "border-color .15s",
-                    position: "relative",
-                  }}
-                >
-                  {/* Imagen o emoji */}
+                <div key={p.id} onClick={() => addItem(p)} style={{
+                  background: C.bg3,
+                  border: `1px solid ${agotado ? C.red + "44" : inCart ? C.neon + "66" : C.border}`,
+                  borderRadius: 12, overflow: "hidden",
+                  cursor: agotado ? "not-allowed" : "pointer",
+                  opacity: agotado ? 0.5 : 1, userSelect: "none",
+                  transition: "border-color .15s", position: "relative",
+                }}>
                   {p.image_url ? (
-                    <img
-                      src={`${API_URL}${p.image_url}`}
-                      alt={p.name}
-                      style={{ width: "100%", height: 90, objectFit: "cover", display: "block" }}
-                    />
+                    <img src={`${API_URL}${p.image_url}`} alt={p.name} style={{ width: "100%", height: 90, objectFit: "cover", display: "block" }} />
                   ) : (
-                    <div style={{
-                      height: 70, display: "flex", alignItems: "center",
-                      justifyContent: "center", fontSize: 30, background: C.bg4,
-                    }}>
-                      {p.emoji}
+                    <div style={{ height: 70, display: "flex", alignItems: "center", justifyContent: "center", background: C.bg4 }}>
+                      <Icon name="bar" size={32} color={C.muted} />
                     </div>
                   )}
-
-                  {/* Badge cantidad en carrito */}
                   {inCart && (
                     <div style={{
                       position: "absolute", top: 6, right: 6,
@@ -187,20 +190,15 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
                       borderRadius: "50%", width: 22, height: 22,
                       fontSize: 11, fontWeight: 700,
                       display: "flex", alignItems: "center", justifyContent: "center",
-                    }}>
-                      {inCart.qty}
-                    </div>
+                    }}>{inCart.qty}</div>
                   )}
-
                   <div style={{ padding: "7px 8px 10px" }}>
                     <div style={{ fontSize: 11, fontWeight: 500, lineHeight: 1.3, marginBottom: 3 }}>{p.name}</div>
                     <div style={{ fontSize: 13, color: C.neon, fontWeight: 600 }}>{fmt(p.price)}</div>
                     {!p.unlimited_stock && p.stock < 5 && p.stock > 0 && (
-                      <div style={{ fontSize: 9, color: C.amber, marginTop: 2 }}>Últimas {p.stock}</div>
-                    )}
-                    {agotado && (
-                      <div style={{ fontSize: 9, color: C.red, marginTop: 2 }}>Agotado</div>
+                      <div style={{ fontSize: 9, color: C.amber, marginTop: 2 }}>Ultimas {p.stock}</div>
                     )}
+                    {agotado && <div style={{ fontSize: 9, color: C.red, marginTop: 2 }}>Agotado</div>}
                   </div>
                 </div>
               );
@@ -208,36 +206,35 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
           </div>
         </div>
 
-        {/* ── Panel de orden ── */}
+        {/* Panel de orden */}
         <div style={{ width: 280, background: C.bg2, borderLeft: `1px solid ${C.border}`, display: "flex", flexDirection: "column", overflow: "hidden" }}>
           <div style={{ padding: "10px 14px", borderBottom: `1px solid ${C.border}`, display: "flex", justifyContent: "space-between", alignItems: "center" }}>
-            <span style={{ fontWeight: 500, fontSize: 13 }}>🧾 Nueva ronda — Mesa {mesa}</span>
+            <span style={{ fontWeight: 500, fontSize: 13, display: "flex", alignItems: "center", gap: 5 }}>
+              <Icon name="receipt" size={14} color={C.muted} /> Nueva ronda — Mesa {mesa}
+            </span>
             <Btn size="sm" variant="ghost" onClick={() => setCart({})}>Limpiar</Btn>
           </div>
 
           {mesaOrder && (
             <div style={{ background: C.amber + "15", borderBottom: `1px solid ${C.border}`, padding: "8px 12px", fontSize: 11, color: C.amber }}>
-              Esta mesa ya tiene una cuenta activa de {fmt(mesaOrder.total)}. Lo que agregues aquí se suma a esa cuenta.
+              Esta mesa ya tiene una cuenta activa de {fmt(mesaOrder.total)}. Lo que agregues aqui se suma a esa cuenta.
             </div>
           )}
 
-          {/* Items del carrito (lo que se va a ENVIAR, no la cuenta completa) */}
           <div style={{ flex: 1, overflowY: "auto", padding: "8px 12px" }}>
             {!cartItems.length && (
               <div style={{ textAlign: "center", color: C.muted, marginTop: 40, fontSize: 12, lineHeight: 2 }}>
-                Sin productos<br /><span style={{ fontSize: 11 }}>Toca del menú</span>
+                Sin productos<br /><span style={{ fontSize: 11 }}>Toca del menu</span>
               </div>
             )}
             {cartItems.map(item => (
               <div key={item.id} style={{ display: "flex", alignItems: "center", gap: 8, padding: "6px 0", borderBottom: `1px solid ${C.border}` }}>
                 {item.image_url ? (
-                  <img
-                    src={`${API_URL}${item.image_url}`}
-                    alt={item.name}
-                    style={{ width: 28, height: 28, borderRadius: 6, objectFit: "cover", flexShrink: 0 }}
-                  />
+                  <img src={`${API_URL}${item.image_url}`} alt={item.name} style={{ width: 28, height: 28, borderRadius: 6, objectFit: "cover", flexShrink: 0 }} />
                 ) : (
-                  <span style={{ fontSize: 16, flexShrink: 0 }}>{item.emoji}</span>
+                  <div style={{ width: 28, height: 28, borderRadius: 6, background: C.bg4, display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
+                    <Icon name="bar" size={14} color={C.muted} />
+                  </div>
                 )}
                 <div style={{ flex: 1, fontSize: 12, lineHeight: 1.3 }}>{item.name}</div>
                 <div style={{ display: "flex", alignItems: "center", gap: 4 }}>
@@ -253,12 +250,8 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
           {/* Totales y envío */}
           <div style={{ padding: "10px 12px", background: C.bg3, borderTop: `1px solid ${C.border}` }}>
             <div style={{ marginBottom: 8 }}>
-              <input
-                value={note}
-                onChange={e => setNote(e.target.value)}
-                placeholder="Nota (sin hielo, bien cocido...)"
-                style={{ width: "100%", background: C.bg4, border: `1px solid ${C.border}`, borderRadius: 6, color: C.text, padding: "5px 8px", fontSize: 11 }}
-              />
+              <input value={note} onChange={e => setNote(e.target.value)} placeholder="Nota (sin hielo, bien cocido...)"
+                style={{ width: "100%", background: C.bg4, border: `1px solid ${C.border}`, borderRadius: 6, color: C.text, padding: "5px 8px", fontSize: 11 }} />
             </div>
             <div style={{ display: "flex", gap: 8, marginBottom: 8 }}>
               <div style={{ flex: 1 }}>
@@ -274,7 +267,9 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
             </div>
 
             {activePromo && (
-              <div style={{ fontSize: 11, color: C.amber, marginBottom: 6 }}>✓ {activePromo.name} aplicada</div>
+              <div style={{ fontSize: 11, color: C.amber, marginBottom: 6, display: "flex", alignItems: "center", gap: 4 }}>
+                <Icon name="check" size={11} color={C.amber} /> {activePromo.name} aplicada
+              </div>
             )}
 
             <Divider />
@@ -284,7 +279,7 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
             </div>
             {(promoDisc > 0 || manualDisc > 0) && (
               <div style={{ fontSize: 12, color: C.amber, display: "flex", justifyContent: "space-between", marginBottom: 3 }}>
-                <span>Descuento</span><span>−{fmt(promoDisc + manualDisc)}</span>
+                <span>Descuento</span><span>-{fmt(promoDisc + manualDisc)}</span>
               </div>
             )}
             <div style={{ fontSize: 14, fontWeight: 600, display: "flex", justifyContent: "space-between", marginBottom: split > 1 ? 3 : 8 }}>
@@ -297,8 +292,8 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
             )}
 
             {sent ? (
-              <div style={{ background: C.neon + "22", border: `1px solid ${C.neon}`, borderRadius: 8, padding: "9px", textAlign: "center", color: C.neon, fontSize: 13, fontWeight: 500, marginBottom: 8 }}>
-                ✓ Enviado a barra
+              <div style={{ background: C.neon + "22", border: `1px solid ${C.neon}`, borderRadius: 8, padding: "9px", textAlign: "center", color: C.neon, fontSize: 13, fontWeight: 500, marginBottom: 8, display: "flex", alignItems: "center", justifyContent: "center", gap: 6 }}>
+                <Icon name="check" size={14} color={C.neon} /> Enviado a barra
               </div>
             ) : (
               <Btn variant="primary" onClick={sendOrder} disabled={!cartItems.length || sending} style={{ width: "100%", fontSize: 14, marginBottom: 8 }}>
@@ -306,11 +301,9 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
               </Btn>
             )}
 
-            {/* El método de pago se decide hasta este momento — cuando la
-                mesa pide la cuenta — no al mandar productos a barra. */}
             {mesaOrder && (
               <Btn variant="amber" onClick={() => setCloseModal(true)} style={{ width: "100%", fontSize: 13 }}>
-                💳 Cobrar cuenta de la mesa
+                <Icon name="card" size={14} /> Cobrar cuenta de la mesa
               </Btn>
             )}
           </div>
@@ -318,7 +311,7 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
       </div>
 
       {closeModal && mesaOrder && (
-        <CloseAccountModal
+        <PayModal
           order={mesaOrder}
           onClose={() => setCloseModal(false)}
           onPaid={() => { setCloseModal(false); onOrdersChanged(); }}
@@ -327,95 +320,3 @@ export default function MeseroView({ user, products, promos, orders, onOrdersCha
     </div>
   );
 }
-
-// Modal de cobro: el mesero elige qué items de la cuenta se están pagando
-// (todos, o solo lo que consumió una persona) y con qué método. Permite
-// pagos parciales: lo pagado se descuenta de lo pendiente de la mesa, y la
-// cuenta sigue abierta hasta que todo esté pagado.
-function CloseAccountModal({ order, onClose, onPaid }) {
-  const pendingIdx = order.items.map((it, i) => ({ it, i })).filter(x => !x.it.paid).map(x => x.i);
-  const [selected, setSelected] = useState(new Set(pendingIdx)); // por default: todo
-  const [pay, setPay] = useState("ef");
-  const [saving, setSaving] = useState(false);
-  const [error, setError] = useState("");
-
-  const toggle = (idx) => {
-    setSelected(s => {
-      const n = new Set(s);
-      n.has(idx) ? n.delete(idx) : n.add(idx);
-      return n;
-    });
-  };
-
-  const selectedTotal = [...selected].reduce((s, idx) => s + order.items[idx].price * order.items[idx].qty, 0);
-
-  const confirmPay = async () => {
-    if (!selected.size) { setError("Selecciona al menos un producto a cobrar"); return; }
-    setSaving(true); setError("");
-    try {
-      await api.payOrderItems(order.id, pay, [...selected]);
-      onPaid();
-    } catch (e) {
-      setError(e.message);
-    } finally {
-      setSaving(false);
-    }
-  };
-
-  return (
-    <Modal title={`Cobrar Mesa ${order.mesa}`} onClose={onClose}>
-      {error && (
-        <div style={{ background: C.red + "22", border: `1px solid ${C.red}`, color: C.red, borderRadius: 8, padding: "8px 12px", fontSize: 12, marginBottom: 10 }}>
-          {error}
-        </div>
-      )}
-      <div style={{ fontSize: 11, color: C.muted, marginBottom: 8 }}>
-        Selecciona qué se va a pagar ahora. Si alguien se va antes, desmarca lo que no es suyo.
-      </div>
-      <div style={{ maxHeight: 220, overflowY: "auto", marginBottom: 10 }}>
-        {order.items.map((item, idx) => (
-          <label key={idx} style={{
-            display: "flex", alignItems: "center", gap: 8, padding: "6px 0",
-            borderBottom: `1px solid ${C.border}`, opacity: item.paid ? 0.4 : 1,
-            cursor: item.paid ? "default" : "pointer",
-          }}>
-            <input
-              type="checkbox"
-              checked={item.paid || selected.has(idx)}
-              disabled={item.paid}
-              onChange={() => toggle(idx)}
-            />
-            <span style={{ flex: 1, fontSize: 12 }}>
-              {item.qty}× {item.name} {item.paid && <span style={{ color: C.neon, fontSize: 10 }}>(ya pagado)</span>}
-            </span>
-            <span style={{ fontSize: 12, color: C.neon }}>{fmt(item.price * item.qty)}</span>
-          </label>
-        ))}
-      </div>
-
-      <div style={{ display: "flex", gap: 4, marginBottom: 12 }}>
-        {PAY_METHODS.map(m => (
-          <button key={m.id} onClick={() => setPay(m.id)} style={{
-            flex: 1, padding: "6px 2px", borderRadius: 6, fontSize: 11,
-            background: pay === m.id ? C.neon2 + "22" : "transparent",
-            border: `1px solid ${pay === m.id ? C.neon2 : C.border}`,
-            color: pay === m.id ? C.neon2 : C.muted, cursor: "pointer",
-          }}>{m.label}</button>
-        ))}
-      </div>
-
-      <Divider />
-      <div style={{ display: "flex", justifyContent: "space-between", fontSize: 15, fontWeight: 600, marginBottom: 14 }}>
-        <span>A cobrar ahora</span>
-        <span style={{ color: C.neon }}>{fmt(selectedTotal)}</span>
-      </div>
-
-      <div style={{ display: "flex", gap: 8 }}>
-        <Btn variant="ghost" onClick={onClose} style={{ flex: 1 }}>Cancelar</Btn>
-        <Btn variant="primary" onClick={confirmPay} disabled={saving} style={{ flex: 1 }}>
-          {saving ? "Cobrando..." : "Confirmar pago"}
-        </Btn>
-      </div>
-    </Modal>
-  );
-}
